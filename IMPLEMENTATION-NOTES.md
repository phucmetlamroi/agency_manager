# IMPLEMENTATION-NOTES — Module Video Review "Team" (Mux + R2 + Inngest)

> Sổ ghi khác biệt giữa spec `docs/review-module/` và repo thật, theo đúng quy trình
> Bước 4 trong `docs/review-module/CLAUDE.md`. Cập nhật mỗi phase.

## P0 (2026-07-04) — Báo cáo verify A1–A13 (KIEN-TRUC.md §1)

| # | Kết quả | Ghi chú |
|---|---|---|
| A1 | ✅ App Router | Next 16.1.6 + React 19.2.3 — chọn version Vidstack/Konva tương thích React 19 ở P4 |
| A2 | ✅ Prisma 5.22 + Neon Postgres | ⚠️ Repo dùng **`prisma db push`** (postinstall chạy `generate && db push` MỌI build Vercel) — KHÔNG dùng `migrate deploy`. SQL thuần (partial unique + CHECK) apply qua `prisma db execute`, artifact tại `prisma/migrations/manual/p0_add_review_module.sql`; Prisma diff bỏ qua expression/partial index + CHECK nên chúng SỐNG SÓT qua db push (đã verify: db push sau khi execute → "already in sync") |
| A3 | ✅ Segment `[workspaceId]` | Route module: `src/app/[workspaceId]/admin/team/` (khu admin hiện có) |
| A4 | ✅ JWT `jose` + `getSession()` | Role ADMIN/USER/LOCKED/CLIENT. `requireReviewAccess()` (src/lib/review/access.ts) bọc `verifyWorkspaceAccess` — không dựng auth thứ hai |
| A5 | ✅ middleware.ts có | Matcher đã loại toàn bộ `/api` → webhook/inngest/cron không cần sửa middleware. Thêm nhánh public `/r/` (copy pattern `/share`: X-Robots-Tag + Referrer-Policy + x-request-id) |
| A6 | ✅ Service đổi status: `updateTaskStatus()` (src/actions/task-actions.ts) validate `isValidStatus` | ❗ **"Sửa lại" KHÔNG tồn tại** — nguồn chân lý `src/lib/task-statuses.ts` `VALID_TASK_STATUSES` (11 giá trị). Mapping chốt (user duyệt 2026-07-04): Request changes ⇒ **`'Revision'`**; Approve ⇒ đề xuất `'Hoàn tất'`. `getReviewStatusOptions()` (P3) đọc động từ VALID_TASK_STATUSES |
| A7 | ⚠️ Nút "Tải video review lên" ĐÃ TỪNG có handler | Là thí nghiệm Cloudflare Stream (VR-P0..P2) — **đã gỡ ở P0-pre** (commit c9efb0b, bảng rỗng đã verify 0 dòng trước khi drop) vì trùng tên model ReviewComment/CommentReaction/CommentAttachment với spec. P1 cắm pipeline mới vào đúng chỗ khối BÀN GIAO |
| A8 | ✅ Pattern composer + toggle nội bộ/công khai + @mention có sẵn (TaskComment GĐ3) | Tái dùng pattern ở P4; mặc định Internal khớp spec |
| A9 | ✅ `@vercel/blob` chỉ cho avatar — không đụng |
| A10 | ✅ Sub-brand = row `Client` con (self-relation parent/subsidiaries) | `brandKey` = id Client con |
| A11 | ⚠️ `vercel.json` ĐÃ có key `crons` (6 jobs) | Merge thêm entry janitor `0 20 * * *` + maxDuration 60s cho `src/app/api/review/uploads/**` (route `complete` P1) |
| A12 | ✅ Chưa có Inngest — cài mới (`inngest` + serve tại `/api/inngest`) |
| A13 | ✅ Radix + Tailwind dark theme đủ dùng lại |

## Khác biệt / quyết định triển khai

1. **Share page:** `src/app/portal/` đã bị GỠ khỏi repo (dự án Canonical Clients) — spec mục "cân nhắc mở rộng portal" hết hiệu lực. Dựng **`/r/[slug]`** đúng Q10. Hệ `ClientShareLink` (portal cả-khách, token 256-bit) và `ShareLink` mới (per-asset review) **song song tồn tại**, không đụng nhau.
2. **FK sang bảng cũ (Task/User/Client/Workspace): SCALAR STRING, KHÔNG FK constraint ở P0** — khác lựa chọn "phương án 1 (relation)" trong KIEN-TRUC §10.2. Lý do: (a) repo có luồng hard-delete đang sống (`deleteTask`, cron `hard-delete-workspaces`) — FK RESTRICT sẽ đổi hành vi các luồng đó ngay khi có dữ liệu review; (b) FK do SQL thủ công tạo sẽ bị `db push` (chạy mỗi build) DROP vì Prisma diff có model hoá FK. Sẽ xem lại ở P1 khi có luồng đọc cần `include`. Toàn vẹn tham chiếu enforce ở service layer.
3. **Bộ SQL thuần (DATA-MODEL §12)** nằm ở `prisma/migrations/manual/p0_add_review_module.sql`; riêng index `path varchar_pattern_ops` khai được trong Prisma (`ops: raw(...)`) nên nằm trong schema (được Prisma quản lý).
4. **Status mapping:** `'Revision'` thay cho "Sửa lại" ở MỌI chỗ spec nhắc (Q11, Flow 6, FR-D02). String hiển thị hoạt động "Khách hàng yêu cầu chỉnh sửa" giữ nguyên như spec (đã có pattern trong app).
5. **Topology deploy (bài học sự cố 2026-07-04):** production deploy từ **main** qua PR merge; push nhánh chỉ tạo preview; postinstall `db push` chạy MỌI build → schema nhánh lên prod DB ngay từ preview build. Hệ quả: thay đổi schema **additive luôn an toàn**; thay đổi **destructive phải chờ code lên main** (đã vi phạm 1 lần → share portal sập ~15 phút, khắc phục bằng restore schema cũ).
6. **Env:** `CRON_SECRET` + `NEXT_PUBLIC_APP_URL` đã có sẵn — không thêm. Danh sách biến mới trong `.env.example`. Email transport thực tế của repo là **Resend** (không phải SendGrid như file đối chiếu ghi) — liên quan P2 notification.

## Trạng thái provisioning (P0) — verify 2026-07-04 (`scripts/probe-review-provisioning.ts`)

- **R2: ✅ HOÀN CHỈNH.** Bucket `hustly-review` tồn tại, credentials hợp lệ; presigned PUT 1MB → đọc được ETag (= MD5 body), presigned GET round-trip OK; **CORS chuẩn** — preflight 204 cho `hustlytasker.xyz` + `localhost:3000` (PUT/GET/HEAD) và response PUT thật có `Access-Control-Expose-Headers: ETag` (bắt buộc cho multipart từ browser). Lưu ý: `GetBucketCors` qua API trả AccessDenied vì token R2 chỉ có quyền Object R/W (không đọc được config bucket) — vô hại, đã verify bằng preflight thật.
- **Mux: ✅ dùng được.** Access Token hợp lệ (GET /video/v1/assets → 200); `MUX_SIGNING_PRIVATE_KEY` bản đủ (2240 ký tự base64 → RSA 2048-bit, ký/verify RS256 OK); webhook secret + signing key id đã nhận. ⚠️ Token **không có scope System** → không verify được `MUX_SIGNING_KEY_ID` qua API (403 "correct scope"); việc ký playback JWT là ký LOCAL bằng private key nên không cần scope này — cặp keyId/privateKey sẽ được xác nhận thực tế khi play video signed đầu tiên (P2/P3).
- **Inngest: ✅ event key hợp lệ** (`inn.gs` → 200 + event id). Signing key đúng format `signkey-prod-<64hex>`; handshake thật diễn ra khi deploy + sync app trên dashboard Inngest.
- **DoD local `/r/bat-ky` → 404 ✅** (kèm noindex + no-referrer + x-request-id; KHÔNG redirect login — đối chứng `/admin` → 307 /login).
- `REVIEW_COOKIE_SECRET`: đã generate (nằm trong `.env` local — mở file để copy). Webhook Mux: bản đầu user tạo trỏ nhầm root `hustlytasker.xyz/` → đã xoá, tạo lại đúng `/api/webhooks/mux` (secret MỚI — Mux không cho sửa URL webhook, secret gắn theo từng webhook).

## ✅ P0 ĐÓNG — DoD end-to-end PASS 14/14 trên production (2026-07-04, `scripts/probe-review-p0-dod.ts`)

Sau khi merge main + env vào Vercel: `/r/bat-ky` prod → 404 noindex không redirect; **PUT /api/inngest → "Successfully registered"**; cron janitor 401-khi-không-auth / 200-với-Bearer; **asset Mux thật** (demo 23s, basic, đã xoá) → webhook `video.asset.ready` về prod, HMAC pass, ledger 1 row, **Inngest function chạy + claim (`processedAt` set)**; replay cùng event id (ký hợp lệ) → `duplicated:true` không thêm row; chữ ký sai → 401 fail-closed. → **Sang P1 (upload pipeline).**
- ✅ **CSP đã nới (P1.8):** cả 2 nhánh `next.config.ts` (ELECTRON_DESKTOP + web) — `connect-src` +`https://*.r2.cloudflarestorage.com https://stream.mux.com https://image.mux.com`, `img-src` +`https://image.mux.com`, `media-src` +`https://stream.mux.com` (giữ `blob:` cho MSE).

## P1.1 + P1.2 — Upload pipeline (2026-07-04)

- **P1.1 lib** (`src/lib/review/`): `media-constants` (MIME allowlist + caps + `mediaKindFromMime`), `upload-helpers` (part-size **tiered 10/20/50MB theo UPLOAD-PIPELINE §4** — KHÔNG theo prose API-SPEC §2.1; `buildR2Key`, `buildSystemKey`, `slugifyBrand`, `looksLikeMedia`, `parseFps`), `r2` (S3 multipart), `mux` (REST hand-rolled, **0 dep mới** — `@mux/mux-node` sẽ khiến `db push` chạy lại mỗi build), `route-auth` (`withReviewRoute` error boundary). DoD probe `scripts/probe-review-upload-lib.ts` 33/33 trên hạ tầng thật (R2 roundtrip + Mux create→ready→delete). ⚠️ target ES2017 → dùng `BigInt(n)` không dùng literal `n`.
- **P1.2 routes**: `POST /api/review/uploads/initiate` (201 mới / 200 replay idempotency-key), `POST …/[id]/complete`, `POST …/[id]/abort`, `GET …/[id]` (poll). Lõi ở `upload-service.ts`; DTO ở `dto.ts`; activity ở `activity.ts`. `sign-parts` KHÔNG là route riêng — refresh URL = gọi lại `initiate` cùng `Idempotency-Key` (đúng spec §2.1). Mux create-asset **dời sang Inngest** (`review/upload.completed`, consumer ở P1.4) để giữ complete <60s.
- **Mô hình concurrency (đã qua 3 vòng review đối kháng, 7+2+2 finding đã fix):** `version.pipelineStatus` là **single source of truth / claim token**. Complete chiếm finalize bằng atomic `updateMany(UPLOADING→UPLOADED)`; abort chiếm bằng `updateMany(UPLOADING→FAILED)` — **cùng 1 row → loại trừ nhau tuyệt đối**. `session.completedAt` = "R2 đã finalize thật" (cổng cho `driveCompletion`, set SAU R2). `driveCompletion` idempotent (atomic flip `UPLOADED→READY|PROCESSING` bầu 1 request ghi activity + gửi Inngest). Abort xóa version có mục tiêu + `reviewAsset.deleteMany({versions:{none}})` (KHÔNG bulk-cascade → không nuốt version anh em đồng thời). R2 finalize lỗi → `revertFinalizeClaim` (UPLOADED→UPLOADING) cho retry. Poll **tự chữa lành** version kẹt UPLOADED (crash giữa completedAt và drive).
- ✅ **P1.6 reconcile ĐÃ LÀM** (`reviewJanitor` 4 sweep; cron `0 20 * * *` + route `/api/cron/review-janitor` đã có sẵn):
  - **(a) abort-expired-uploading** — session quá 24h, version vẫn `UPLOADING` → `expireInflightUpload`: free R2 (abort multipart / delete object) + flip `UPLOADING→FAILED` (GIỮ row để user thấy "failed & retry", khác abort chủ động = xoá row).
  - **(b) redrive-stuck-uploaded** — version kẹt `UPLOADED` >15′ (crash sau khi claim finalize): `reconcileStuckUploadedVersion` — `completedAt` set **hoặc** `headObject` thấy object trên R2 → `driveCompletion` (forward, C1/C2); object mất + session hết hạn → FAILED.
  - **(c) reconcile-processing** — video kẹt `PROCESSING` >20′: `muxAssetId` null → re-emit `UPLOAD_COMPLETED` (retry create-asset), quá **24h** vẫn null → FAILED; có `muxAssetId` → `getMuxAsset`: ready→apply, errored/404→FAILED, `preparing` quá 24h → FAILED + **xoá Mux asset kẹt** (CHỈ khi flip được sang FAILED và asset còn khớp → không đụng version đang READY).
  - **(d) reenqueue-webhooks** — `WebhookEvent processedAt=null` trong [1h, 7d) → re-emit `MUX_EVENT_RECEIVED`; quá 7d → đếm + log `webhooks_abandoned` (ngừng re-enqueue).
  - Refactor: rút `applyMuxReady`/`applyMuxErrored` dùng chung webhook + reconcile (1 nguồn transition, không drift). `applyMuxReady` phân biệt terminal `FAILED/READY` (→ consume event) vs pre-ready thật (→ throw-retry) — **vá loop vô hạn** re-enqueue webhook-ready-cho-version-FAILED. Cạnh đó vá `store-mux-id` (Inngest retry không xoá nhầm Mux asset đang live). Cycle import `inngest ↔ upload-service` là **call-time-only** (build xanh).
  - Verify: tsc + `next build --webpack` xanh; **review đối kháng 2 vòng** (4 finder × 2 skeptic, rồi 2 skeptic hội tụ) — 3 finding thật đã fix + 1 TOCTOU tự bắt+vá, tất cả hội tụ CLEAN. Janitor cần Inngest runtime nên không probe tsx được.
- **DTO probe** `scripts/probe-review-dto.ts` 24/24 (serializeVersion/status maps/toUserRef — phần test được không cần session). Routes cần session cookie nên KHÔNG probe e2e qua tsx được (giới hạn `getSession()`); verify = tsc + build + review đối kháng.
- ✅ **P1.7 — playback token + download ĐÃ LÀM** (API-SPEC §2.9):
  - `mux-jwt.ts` — **Mux signed JWT RS256 tự ký** bằng `node:crypto` (0 dep mới): header `{alg:RS256, typ:JWT, kid:MUX_SIGNING_KEY_ID}`, payload `{sub:playbackId, aud, exp}` (không iat, +6h), key = `MUX_SIGNING_PRIVATE_KEY` (base64 PEM, tự decode; nhận cả raw PEM). `mintPlaybackTokens` trả 3 token aud `v`/`t`/`s` cùng exp.
  - `POST /api/review/versions/:id/playback-token` (member) → `getVersionPlaybackTokens`: 404 nếu không có version, 409 nếu không phải VIDEO / chưa READY / thiếu `muxPlaybackId`; re-check workspace từ `version.workspaceId` (chặn chéo tenant); token bind theo `sub=playbackId`.
  - `POST /api/review/versions/:id/download-url` (member) → `getVersionDownloadUrl`: presigned R2 GET 15′ của bản gốc, chỉ khi READY + có `r2Key`; nội bộ KHÔNG có cổng approval (đó là của khách §5.5.7).
  - `media-links.ts` `buildMediaLinks` → poster (`image.mux.com/{id}/thumbnail.webp?time=&token=`) + storyboard vtt; **wired vào `getUploadStatus`** chỉ khi READY + có playback id (ảnh/processing → null). Không log token/key.
  - Verify: probe crypto `scripts/probe-review-mux-jwt.ts` **30/30** (RS256 sig verify bằng public key + bắt tamper + header/payload/aud/exp đúng); tsc + build xanh; **review đối kháng bảo mật 2 lens (auth-leak + state-spec) → 0 finding**. Route cần session nên không probe e2e được.
- ✅ **P1.9 — Client upload engine + store ĐÃ LÀM** (UPLOAD-PIPELINE §3; **client-side, out-of-React**):
  - `upload-store.ts` — store observable **0 dep (không zustand)**: snapshot **ổn định tham chiếu** cho `useSyncExternalStore` (getSnapshot chỉ đổi ref khi có mutation, per-item ref giữ nguyên nếu không đổi → không vòng lặp render), `getServerSnapshot` = hằng rỗng (SSR). Chỉ giữ **state hiển thị serializable** (File/XHR/ETag/timer nằm trong runtime private của engine). Selector thuần: `aggregateProgress`, `etaSeconds`, `hasBlockingUploads`, `isActiveStatus`, `formatBytes/formatDuration`.
  - `upload-engine.ts` — quản lý multipart out-of-React: **4 part/file · 3 file · cap 8 part in-flight toàn cục**; retry per-part (network/timeout/429/5xx, backoff `min(30s,1000·2^n)+rand`, tối đa 5); **403 → re-sign bằng gọi lại initiate cùng Idempotency-Key**; retry chỉ up **part còn thiếu** (giữ ETag); pause/resume/cancel; **watchdog 120s** không progress → abort+retry; `window` **offline→pause / online→resume** (chỉ item network-paused); **beforeunload** chặn khi đang truyền; complete → **poll 3s** tới ready/failed. Byte KHÔNG qua server (XHR PUT thẳng R2 + progress); control-plane = fetch cookie same-origin.
  - **Contract khớp server:** single-part (parts.length≤1) gửi `Content-Type` (presignPutObject có ký); multipart **không** gửi (presignUploadPart không ký) — tránh SignatureDoesNotMatch. sizeBytes = chuỗi số nguyên. Route theo target: folder/asset → `/uploads/initiate`, task → `/task-upload/initiate` (nhận thêm `folderPath`, `createdNewAsset`).
  - **Bộ lập lịch reentrancy-safe** (guard `scheduling`/`rescheduleRequested` — pass không lồng nhau); kế toán slot đối xứng mọi nhánh (ok/retry/expired/fatal/pause/cancel/timeout/missing-url/throw); `failFile` xoá khỏi `activeFiles` TRƯỚC khi abort part anh em (abort đồng bộ → không relaunch file đang fail); giải phóng `File` khi done/cancel/**bỏ-poll-quá-1h**.
  - Verify: probe thuần `scripts/probe-review-upload-engine.ts` **83/83** (planParts/backoff/retryable/byte-accounting/EMA/validate/store snapshot+selectors); tsc + `next build --webpack` xanh; **review đối kháng 4 lens (concurrency/lifecycle/contract/store) → 4 finding CONFIRMED đã fix**: (1) MIME rỗng .mkv bị server `min(1)` từ chối → coerce `application/octet-stream`; (2) poll bỏ cuộc sau 1h rò `File`+kẹt 'processing' → `abandonPoll` giải phóng runtime; (3) `.catch` 403-resign hồi sinh row đã cancel → guard cancel/pause; (4) `refreshPartUrls` ghi state sau await không re-check → guard sau await. Thêm 2 hardening (pump sau remove; reset attempt budget khi resume). Engine cần trình duyệt (XHR/window) nên phần I/O verify bằng review + preview ở P1.10.
- ✅ **P1.10 — BÀN GIAO strip + card review trong drawer + Upload Tray ĐÃ LÀM** (UI-UX-SPEC §5 + §8; nối engine P1.9 vào drawer task):
  - **Read route mới** `GET /api/review/tasks/:taskId/assets` (`task-assets.ts` → `getTaskAssets`, force-dynamic cho poll 3s): trả `assets[]` (deliverable + version DTO + media link ký khi READY) + `uploadContext` (breadcrumb đích, cờ `parsedOk`, `existingAsset` → v{n+1}). **Bảo mật:** load task rồi `requireReviewAccess({workspaceId})` **trước khi trả data** (khớp `initiateTaskUpload`, không lộ chéo tenant); DTO **không** có trường tiền/assignee (model review vốn không có). `parse-task-context.ts` thêm cờ `matched` (điều khiển cảnh báo "không nhận diện Khách/Brand").
  - **`use-upload-store.ts`** — binding React qua `useSyncExternalStore` (`useUploadState/useUploadItems/useTaskUploads`), memo trên snapshot ổn-định-ref → không vòng lặp render.
  - **`TaskReviewUploadSection.tsx`** (khối "🎬 Video review" trong card "Bàn giao"): nút/drop-target → **confirm strip** (đích + "Sẽ tạo v{n+1}" hoặc cảnh báo vàng nếu parse hụt) → `enqueue({kind:'task',taskId})`; card vòng đời **Đang tải N% → Đang xử lý → Sẵn sàng (thumbnail Mux + vN + trạng thái review + người tải) | Lỗi + Thử lại**; **dedupe theo versionId** (item store live che version tương ứng ở server, không double-render); **poll 3s** khi còn item/version đang chạy; validate video-only + coerce MIME rỗng trước khi enqueue.
  - **`UploadTray.tsx`** — Upload Sheet toàn cục góc phải-dưới, mount trong `AdminShell` (sống qua điều hướng): header batch %, per-file (tên/đích/%/tốc độ/ETA), pause/resume/cancel/retry/remove theo trạng thái, "Xóa mục đã xong", thu gọn thành pill, "Hủy tất cả" (inline-confirm, chỉ hủy row đang chạy), cancel >50% có `window.confirm`. `beforeunload` đã ở engine.
  - **Nối additive:** chèn section vào card "Bàn giao" của `TaskDetailModal` (**luồng dán link cũ giữ nguyên**); mount `<UploadTray/>` trong `AdminShell`.
  - **Chủ ý hoãn đúng phase** (không dựng nút chết): "Đổi vị trí…" (folder picker) + override tay Khách/Brand/Video → P2 (cần Team browser + initiate nhận override); nút "Mở review"/"Copy link khách" trên card ready → P4/P5 (card ready hiện dạng thông tin). Trạng thái `interrupted` (reload mất File) chưa phát sinh (store in-memory) → chưa cần UI riêng.
  - Verify: tsc + `next build --webpack` xanh (route mới đã đăng ký) + probe engine **83/83** giữ nguyên; **review đối kháng 4 lens (security/react/logic/spec)**: security & react **0 finding**; **3 finding CONFIRMED đã fix**: (1) **[High]** preview "Sẽ tạo v{n+1}" tính từ head pointer trong khi upload đánh số từ `MAX(versionNumber)` → lệch khi có version non-head (v2 đang PROCESSING/FAILED) → **fix: aggregate `_max.versionNumber`** khớp `initiateUpload`; (2)+(3) **[Low]** "Hủy tất cả" lặp `cancel()` mọi row kể cả 'done' → đổi nhãn "Đã hủy" → **fix: lọc loop + guard `cancel()` chặn terminal done/canceled**. Khối BÀN GIAO session-gated (sau đăng nhập + mở drawer) nên preview headless không tự vào được → verify I/O thật bằng review + test tay trên app.
- ✅ **P2.5 — Context menu + rename/move/copy/duplicate/download + multi-select ĐÃ LÀM** (PRD FR-B07/B08/B11, API-SPEC §1.5–1.7):
  - **Điều chỉnh repo-vs-spec (QUAN TRỌNG): Copy/Duplicate = copy-on-reference.** API-SPEC §1.7 chốt "tạo row mới trỏ CÙNG `r2Key` + `muxAssetId`/`muxPlaybackId`". NHƯNG schema thực tế đặt `muxAssetId @unique` → bản sao KHÔNG thể dùng chung `muxAssetId`. **Giải: bản sao để `muxAssetId = null`, dùng chung `muxPlaybackId` + `r2Key` + toàn bộ metadata.** Mọi đường phục vụ (playback JWT / thumbnail / storyboard / download bản gốc) đều khoá theo `muxPlaybackId`/`r2Key` nên bản sao vẫn stream + tải bình thường; `muxAssetId` chỉ dùng để đối chiếu webhook + purge ref-count (việc của P6). Giữ đúng Ý ĐỊNH spec (instant, $0, không copy object/re-encode), chỉ đổi chi tiết kỹ thuật. **Chỉ sao chép head version READY** — asset chưa xử lý xong bị BỎ QUA (folder copy, đếm `skippedAssets`) hoặc TỪ CHỐI (copy 1 asset), tránh bản sao kẹt 'processing' vĩnh viễn (nó không nhận được webhook Mux của bản gốc). Không copy comment/share/version cũ (bản sao sạch).
  - **`copyItems` batch 2-pha** (`folders.ts`): plan toàn bộ cây con mới trong bộ nhớ (đọc đệ quy qua materialized path — vài query), rồi materialize bằng `createMany` folders/assets/versions + 1 raw UPDATE nối `currentVersionId` (FK vòng: asset↔version) → **~6 query bất kể kích thước**, không đụng timeout tx/function. Cap `MAX_COPY_ITEMS=500`. Chống chu trình: cấm copy folder vào chính nó/con (`target.path.startsWith(source.path)`). Rollup `itemCount`+`totalSizeBytes` (đệ quy deepest-first) + cộng byte lên chain tổ tiên đích. Duplicate = copy vào cùng parent, hậu tố `" (copy)"` (folder qua uniqueTopName giữ "(2)").
  - **`renameAsset`** (`PATCH /api/review/assets/:id`, optimistic-lock `rowVersion`) — đổi `name` card, KHÔNG đụng `fileName` version. **`getFolderManifest`** (`GET /api/review/folders/:id/manifest`) — liệt kê head version READY của cây con + relPath; client tải tuần tự qua route `download-url` sẵn có (mỗi lần re-check access + presign). Route mới: `POST /items/copy`.
  - **Quyền xoá folder (FR-B07):** enforce SERVER trong `deleteItems` — USER chỉ xoá folder mình tạo, ADMIN xoá mọi folder (asset không giới hạn); UI cũng disable mục Xóa cho folder người khác tạo (truyền `currentUserId`+`isAdmin` từ page qua `getSession()`).
  - **UI:** 1 Radix ContextMenu điều khiển (resolve target qua `data-review-id`/`data-review-type` — hoạt động cho cả grid card, list row, canvas trống); menu folder 9 mục / asset 10 mục **đúng thứ tự PRD**, mục share (1–2) + Manage Versions (asset 3) **disabled** ("bản sau"), KHÔNG có mục đã loại (Manage Access/Make Restricted/Open on Desktop/Compare Versions/Generate Transcripts/Upload Caption). Multi-select: click/Ctrl-toggle/Shift-range/checkbox-hover/select-all/Esc, **cap 200**; bottom SelectionBar (copy §1.4.7 asset/folder/mix + tổng dung lượng + runtime) với bulk Download/Move/Copy/Delete. Rename inline (F2 hoặc menu). MoveCopyDialog tree-picker (disable folder nguồn + con, chống chu trình). Deep-link `?asset=` preselect. Dep mới: `@radix-ui/react-context-menu`.
  - **KHÔNG đụng schema** (chỉ dùng model P2.1 sẵn có) → branch push = preview an toàn. Verify: `tsc --noEmit` + `next build --webpack` **xanh (0 lỗi, 3 route mới đăng ký)**; test tay chờ checkpoint (route session-gated). Trash VIEW + restore = P2.6.
  - **Review đối kháng đa-agent (4 chiều × skeptic phản biện): 5 finding CONFIRMED → đã fix hết:** (1) **[HIGH] leo thang quyền** — `requireReviewAccess({workspaceId})` tính `isAdmin` từ role GLOBAL của JWT thay vì role theo-workspace → user global-ADMIN nhưng chỉ MEMBER của workspace này vượt được guard xoá folder người khác (qua API trực tiếp) → **fix: `access.ts` lấy `isAdmin` từ `workspaceRole`/`profileRole` (đúng predicate `verifyProfileAdminAccess`), nhánh no-workspace trả `false`; 2 page tính `isAdmin` client qua `verifyProfileAdminAccess` try/catch** (verify độc lập 6/6 PASS); (2) **[MED]** nút Xóa ở SelectionBar không gate quyền → thêm `canDelete`; (3) **[MED]** menu chuột-phải ASSET bỏ qua `canDelete` khi chọn hỗn hợp có folder người khác → gate luôn; (4) **[LOW]** checkbox "chọn tất cả" kẹt không bỏ chọn được khi >200 mục → toggle theo `anyVisibleSelected`; (5) **[LOW]** upload xong `silentRefresh` MERGE hồi sinh row vừa xoá/di chuyển → `removedRef` chặn tới khi `silentReplace` xác nhận. (Lưu ý: route Team đã admin-only ở layout `/admin/*` — guard FR-B07 chủ yếu bảo vệ bề mặt API trực tiếp.)
- ✅ **P2.6 — Recently Deleted (Trash view) ĐÃ LÀM** (FR-B13 view + restore). Backend `listTrash`/`restoreItems` + route `/api/review/trash` + `/trash/restore` **đã có sẵn từ P2.1** → P2.6 chỉ là UI: route `/{workspaceId}/admin/team/trash` (page → `<TeamTrash>`) + `src/components/review/TeamTrash.tsx` + entry nút Thùng rác (Trash2) trên toolbar Team + `apiRestoreItems` trong team-actions. Mỗi subtree xoá = 1 entry (deleteBatchId); banner 30 ngày; countdown "Còn n ngày" (client `Date.now()`, floor 0, NaN-safe); người xóa; Restore đơn + hàng loạt (select-all) → toast, movedToRoot → "về gốc Team vì thư mục gốc đã bị xóa"; mục `restorable:false` (orphanedFromPurge) disable chọn+restore. KHÔNG có xóa-vĩnh-viễn/purge (= P6 cron). **Review đối kháng (1 agent tập trung): 1 finding thật + 1 low → đã fix:** (a) **[MED] cắt trang câm** — list chỉ tải page-1 (limit 60), KHÔNG theo `nextCursor` → entry thứ 61+ không thể khôi phục trước khi purge → **fix: Load-More ("Tải thêm") tích lũy theo cursor + cap chọn 200 (khớp giới hạn route restore)**; (b) **[LOW] race guard đôi** (row `busy` + bulk riêng) có thể bắn 2 call restore trùng → **fix: 1 guard `restoringKey` toàn cục disable mọi nút restore khi đang chạy**. Verify: `tsc` + `next build --webpack` xanh (route `/admin/team/trash` đăng ký). **→ P2 (Team browser) coi như XONG các deliverable chính (P2.1→P2.6); còn lại chỉ vài polish nhỏ (thả file vào folder-card) + player = P4.**
- ✅ **P2.7 — Verify hợp nhất + đóng DoD P2 ĐÃ LÀM.** Audit checklist ACCEPTANCE-TESTS phát hiện 2 gap DoD thật → vá: (1) **FR-B02 AC1 breadcrumb `…`** — trước chỉ truncate từng crumb (sâu 8 cấp tràn ngang) → thêm `BreadcrumbTrail` gộp cấp giữa thành dropdown `…` (Radix) khi >4 cấp; (2) **TRS-01 undo 5s** — toast xoá giờ có nút **Hoàn tác** (5–6s) gọi `apiRestoreItems`, và đổi string thành `Đã chuyển vào "Đã xóa gần đây".` đúng spec. Thêm **probe headless** `scripts/probe-review-p2-dod.ts` (**26/26 PASS**): (a) không rò trường tiền trong DTO/serializer review (strip comment trước khi quét — jobPriceUSD chỉ nằm trong comment "no leak"); (b) context menu đúng số mục folder 9 / asset 10 / canvas 3 + KHÔNG có 6 mục đã loại; (c) đủ 7 route + 3 page; (d) fix `isAdmin` theo workspace + guard FR-B07 + copy-on-reference `muxAssetId=null`. Doc `docs/review-module/04-KE-HOACH-TRIEN-KHAI/P2-DOD-STATUS.md` = bảng deliverable→hiện thực, đối chiếu DoD, **checklist thao tác tay** (BRW-01..09 + TRS-01..03 Blocker; BRW-06..08 Major) cho phần session-gated, và bảng **hoãn có chủ đích** (folder-card drop + drag→version + menu `…` hover = P3; player/lightbox + status dropdown = P4/P3; share menu = P5; purge/Delete-forever = P6). Verify: tsc + build xanh + probe 26/26. **P2 KHÉP LẠI. NEXT: P3 (version stack v1/v2/v3 + status asset đọc động + sync task thủ công).**
- 🔧 **P3.1–P3.3 BACKEND ĐÃ LÀM (2026-07-06)** — version stack + status + sync task. **PHÁT HIỆN LỚN: P3 KHÔNG cần schema mới** — `ReviewAsset`/`ReviewVersion`/`currentVersionId`/`versionNumber`/`reviewState`/`statusId` + comment-theo-`versionId` + model **`ReviewActivity`** (lịch sử status, KHÔNG tạo `ReviewStatusHistory` — DATA-MODEL §4.10 thắng ROADMAP về thẩm quyền 03>04) đều có sẵn từ P0/P1. Branch push=preview an toàn, merge=prod an toàn (thuần additive code). **`versions.ts`** (nhóm C): `listVersions` (new→old + comment count/version), `deleteVersion` (soft-delete 1 version; xoá version cuối → **xoá luôn cả stack** I2; head → re-point currentVersionId next-highest LIVE I3; KHÔNG đánh lại số), `removeFromStack` (tách version thành asset mới cùng folder — comment đi theo qua versionId; giữ số version, gap OK; itemCount+1, byte net-zero), `mergeStacks` (kéo asset đè asset: dời live versions của nguồn vào đích đánh số max+1.. — max ĐẾM cả bản đã xoá; re-point head; soft-delete asset nguồn; chặn khi có version đang upload dở "Chờ upload hiện tại hoàn tất"; cấm gộp ảnh↔video; byte-rollup chéo folder). **`status.ts`** (nhóm D-01): `getReviewStatusOptions` đọc động `VALID_TASK_STATUSES` (KHÔNG hardcode), `setAssetStatus` (validate `isValidStatus`, optimistic-lock, ghi ReviewActivity STATUS_CHANGED {old,new}). **`status-map.ts`** = 1 config map approved→'Hoàn tất', changesRequested→'Revision'. **`task-sync.ts`** (nhóm D-02): `confirmTaskHoanTat(taskId)` — guard có asset approved cho task rồi **DELEGATE `updateTaskStatus`** (service app đã tự lo RBAC admin/assignee + FSM transition + optimistic-lock + revalidate; chỉ select field non-finance) + ghi activity. Route mới: `GET assets/:id/versions`, `POST assets/:id/stack`, `PUT assets/:id/status`, `DELETE versions/:id`, `POST versions/:id/remove-from-stack`, `GET statuses`, `POST tasks/:taskId/confirm-complete`. Export thêm `pathIds`/`addBytesToAncestors`/`liveStackBytes` từ folders.ts cho versions.ts dùng chung; thêm 5 event vào `REVIEW_ACTIVITY`. Verify: tsc + build xanh (7 route đăng ký). **CÒN LẠI P3 = UI: P3.4 Manage Versions modal, P3.5 status dropdown, P3.6 drag→version (thả file/kéo asset đè asset), P3.7 banner sync trong drawer task; rồi P3.8 verify+review.**
- ✅ **P3.4–P3.7 UI ĐÃ LÀM + P3.8 verify (2026-07-06)** — thuần additive code, KHÔNG schema. **P3.4** `ManageVersionsModal.tsx` (Radix Dialog ~900px): list version mới→cũ (thumb/badge v{n}/"Hiện tại"/tên file gốc/người tải·ngày/dung lượng/💬count), menu `…` mỗi dòng = Tải xuống / Tách khỏi stack / Xóa phiên bản (2 mục Tách+Xóa **ẩn khi chỉ 1 version**), confirm overlay in-modal, xóa version cuối → `stackDeleted` → đóng modal + refresh grid; NO reorder; click thumbnail = onOpenVersion (P4 → toast). Bật mục "Quản lý phiên bản" trong TeamContextMenu (asset đơn) + SelectionBar (sole asset) + nút "Phiên bản" trong InfoPanel. **P3.5** `StatusControl.tsx` (Radix Popover, searchable, màu app qua `view-prefs.statusColor`, "Bỏ trạng thái", options đọc động từ `/api/review/statuses` cache module-level) thay `StatusChip` placeholder trên card/list/InfoPanel; `doSetStatus` optimistic + optimistic-lock rowVersion + rollback lỗi; trigger stopPropagation không select/mở card. **P3.6** drag-drop (`ItemDnd`/`REVIEW_ITEMS_MIME` trong TeamCards): thả file OS lên asset-card → version mới (`uploadEngine.enqueue {kind:'asset'}`), kéo asset đè asset → confirm → `mergeStacks`, kéo item đè folder → move; indicator riêng folder=move / asset=version|merge; chặn khi asset đang upload (`busyAssetIds` từ `useUploadItems`) + tooltip "Chờ upload hiện tại hoàn tất"; `assetFileHover` ẩn DropOverlay folder khi hover card; `assetUploadSig` refresh grid khi version xong. **P3.7** banner "Chuyển task sang Hoàn tất?" trong `TaskReviewUploadSection` khi linked asset `statusId === REVIEW_STATUS_MAP.approved && taskStatus !== approved` → `apiConfirmTaskComplete`; `TaskDetailModal` truyền `taskStatus={localTask.status}` + `onTaskCompleted` sync `setLocalTask` (đổi `task?.id`→`localTask?.id`). Client wrappers mới trong team-actions: `listAssetVersions/apiDeleteVersion/apiRemoveFromStack/apiMergeStacks/fetchStatusOptions/apiSetAssetStatus/apiConfirmTaskComplete`. Generalize `ConfirmModal` (tone danger|primary + icon) — path xóa cũ giữ nguyên. **Review đối kháng đa-agent (4 chiều × skeptic, 18 agent): CHỈ 1 finding CONFIRMED (medium) → đã fix:** race `silentRefresh`/`silentReplace` MERGE ghi đè status optimistic đang bay (upload xong bắn silentRefresh giữa lúc PUT status chưa về → chip revert-rồi-flicker; server vẫn authoritative, không mất data) → **fix: `pendingStatusRef` (Map assetId→optimistic statusKey) set trước optimistic + delete ở `finally`; `silentRefresh`/`silentReplace` re-apply pending status lên fresh assets (cùng tinh thần `removedRef` cho row đã xóa)**. Verify: tsc + build xanh (2 lần). **P3 XONG (P3.1→P3.8). Còn hoãn có chủ đích: click version = player deep-link (P4); DnD ở list-view chỉ có status, không kéo-thả (grid là bề mặt DnD — context menu vẫn move/copy/manage được ở list).** NEXT = P4 (player Vidstack + timecode comment + Konva annotation).
- 🔧 **P4.1–P4.3 + entry-point + review (2026-07-06) — PLAYER + COMMENT LOOP** (nội bộ; thuần additive code, KHÔNG schema mới — model comment/annotation `ReviewComment`/`CommentAttachment`/`CommentReaction` đã có sẵn từ P0). **2 điều chỉnh repo-vs-spec lớn (rule #4):**
  - **Player = native `<video>` + hls.js, KHÔNG Vidstack React wrapper.** `@vidstack/react` tag `latest`=0.6.15 khoá peer React ^18 (1.x chỉ ở tag `next`), repo là **React 19 / Next 16** → wrapper là rủi ro compat + CSP thật; `<video>` thô cho điều khiển `requestVideoFrameCallback` trực tiếp (yêu-cầu-đinh E02 frame-accurate). Giữ `hls.js` (phần tử stack đã chốt). iPhone Safari → native HLS (Auto-only).
  - **Annotation sẽ render bằng SVG, KHÔNG Konva.** DATA-MODEL §6 dòng 734 cho phép rõ "render đè bằng **SVG/Konva**"; react-konva (canvas reconciler) là dep rủi ro nhất trên React 19. Cùng JSON schema (tool/color/size, toạ độ 0–1, đúng 4 màu, perfect-freehand cho pen). Dep cài `--ignore-scripts` (postinstall = `prisma db push`, tránh chạm DB): +`hls.js` +`swr` +`perfect-freehand`.
  - **P4.1 backend** (API-SPEC §4): `annotation.ts` (zod 4-tool/4-màu, cap 100 shapes/2000 điểm, DbNull khi rỗng), `dto.ts` +`CommentDto`/`serializeComment` (frame↔ms theo fps rational, reactions gộp, **attachment URL ổn định** = route `/comment-attachments/:id/raw` 302→R2 ký lại, không churn chữ ký mỗi poll), `comments.ts` (list +since-delta/sort/filter/search/otherVersions, create +reply-ép-visibility +range +annotation +**attachment claim-by-R2-key** ownership qua userId, edit tác-giả, delete tác-giả|admin cascade reply, resolve/unresolve, reactions idempotent). 7 route. `commentCount` tăng/giảm trên version (incl. cascade reply). Mọi mutation re-verify version→asset→workspace→`requireReviewAccess` (chống chéo tenant); CommentDto **không** trường tiền.
  - **P4.2 player** (FR-E01/E02): route `team/asset/[assetId]?v=&comment=`; `timecode.ts` (SMPTE HH:MM:SS:FF từ frame int + fps rational, 29.97 non-drop; seek mid-frame cho ±1/±10); `useHlsPlayer` (hls attach dynamic-import + 403 token-refresh, rVFC frame tracking + rAF fallback, play/seek/step/rate/quality/mute); `PlayerControls` (scrub + hover-scrub thumbnail Mux tái dùng token poster, timecode SMPTE, speed preset, quality pin ẩn khi native-HLS, frame-step ±1/±10 → touch ±1s, fullscreen); `VideoStage` (letterbox 9:16, Fullscreen API + pseudo-fs iPhone, ảnh fallback qua download-url, slot annotation P4.4 + markers P4.3); `ReviewPlayerShell` (load stack, version selector không reload, tab Bình luận/Thông tin).
  - **P4.3 comment loop** (FR-E03/E08/E09/E11): `comment-client.ts` wrappers; `useComments` (SWR poll 5s — SWR tự dừng interval khi tab ẩn → thoả "tab ẩn dừng poll"; optimistic patch + reconcile); `CommentComposer` (gõ auto-pause video, chip timecode bám playhead, toggle bỏ mốc, selector Nội bộ/Công khai mặc định Nội bộ nhớ session, Enter gửi + focus về player); `CommentItem` (thread card — chip timecode click→seek, resolve mờ card, reactions 6 emoji, reply kế thừa visibility, edit tác-giả / delete tác-giả|admin, chip annotation+attachment); `TimelineMarkers` (chấm/comment timecoded trên playbar: nội bộ vàng / resolved mờ / công khai chàm; click seek+highlight); `CommentsPanel` (list thread + empty-state "N bình luận ở version khác [Xem]" → nhảy version mới nhất có comment); shell nối feed + markers + phím Space/←/→ + deep-link `?comment=` (seek+scroll+highlight).
  - **Entry-point:** double-click asset (grid+list) trong Team → `openAsset` điều hướng `team/asset/[id]` (trước là toast "sắp có").
  - **Review đối kháng đa-agent (3 lens × skeptic refute-mặc-định, 14 agent): 8/11 finding CONFIRMED → đã fix hết** (3 refuted, gồm 1 claim "poll 5s đè optimistic" bị bác vì SWR 2.4 có mutation-race guard + 1 chia-cho-0 fps bất khả đạt): **[high]** currentSec setState mỗi frame → re-render cả shell theo frame-rate → coalesce currentSec theo frame-change + **React.memo(CommentThread)** + memo actions/onPosted + playhead ổn định cho thread (list bail khi phát); **[med]** cap 10MB attachment chỉ ở metadata client → bypass → **re-check `head.size` R2 thật khi claim**; **[med]** refresh token 403 lặp vô hạn → **cap 2 lần/load** (reset ở MANIFEST_PARSED) rồi báo lỗi; **[med]** pseudo-fullscreen kẹt viewport desktop khi requestFullscreen bị từ chối → nút luôn clear pseudoFs trước; **[med]** reconcile reaction đè reaction song song → **bỏ reconcile success**, chỉ delta optimistic + poll; **[low]** off-by-one `startFrame/endFrame == totalFrames` → `>= tf`; **[low]** effect keydown/deep-link re-subscribe mỗi render (dep cả controller) → dep theo method refs ổn định; **[low]** comment mới append cuối trái thứ tự server → **panel tự sort parents theo timecode**. Verify: tsc + `next build --webpack` xanh (nhiều lần). Player session-gated + cần Mux/R2 thật nên không preview headless → dựa tsc+build+review (đồng nhất DoD P2/P3).
  - **CÒN LẠI trong P4 (chưa xong):** **P4.4 range brackets + SVG annotation overlay (đinh E05)** + **P4.5 emoji picker + đính ảnh ≤6/≤10MB + nút "Mở review"/đếm "chưa xử lý" ở drawer task**; panel sort/filter/search [S] có thể trượt P6 (roadmap cho phép). NEXT session = P4.4 annotation.
- ✅ **P4.4 — Range brackets + SVG annotation overlay ĐÃ LÀM (2026-07-06) — ĐINH E05** (nội bộ; **thuần frontend, KHÔNG schema/API mới** — contract `startFrame`/`endFrame`/`annotation` đã hoàn chỉnh từ P4.1 backend). **Điều chỉnh repo-vs-spec:**
  - **Render annotation bằng SVG, KHÔNG Konva** (đã chốt ở P4.1; nay hiện thực): react-konva rủi ro trên React 19; SVG không cần dep. Toạ độ **chuẩn hoá 0..1 theo KHUNG GỐC** → cùng hình vẽ render đúng ở mọi kích thước player / fullscreen / 9:16.
  - **Annotation chỉ cho VIDEO ở phase này** (ảnh tạm hoãn). Backend cho phép annotation trên ảnh (không cần timecode), nhưng UI ảnh chưa có ref khung + khái niệm "frame" → nút Vẽ ẩn khi mediaKind=image. Bổ sung sau nếu cần (chi phí thấp).
  - **File mới:** `annotation-geometry.ts` (thuần: `containedBox` = rect letterbox object-fit-contain, `clamp01`, `viewBoxHeight`, `arrowHeadPoints`); `useAnnotation.ts` (draw state ở SHELL: tool/color/size + shapes + undo/redo/clear/reset + `frame` ghim; in-progress stroke KHÔNG ở đây → không re-render shell khi kéo); `AnnotationCanvas.tsx` (1 `<svg>` đặt đúng content-box, viewBox `0 0 1000 vbh` → `strokeWidth=size` khớp đơn vị 1/1000-width + điểm chuẩn hoá map trực tiếp; pen/line/arrow/rect; ResizeObserver theo dõi stage; draft cục bộ, commit khi pointer-up); `AnnotationToolbar.tsx` (4 tool + 4 màu khoá + 3 cỡ nét + undo/redo/clear + thoát).
  - **Composer (P4.4b):** nút **Vẽ** (video, top-level) → `annotation.begin(frame)` (pause + freeze frame = startFrame, ép timecode on); chỉ báo "Đang vẽ · N"; **Khoảng (range In–Out):** nút Brackets set `rangeEnd` = playhead, "Đặt cuối" cập nhật; submit gắn `endFrame` khi `rangeEnd > startFrame`; **cho phép comment CHỈ-hình-vẽ** (body optional — server vốn nhận body||annotation||attachment). Sau gửi → `annotation.reset()` + clear range.
  - **Shell (P4.4c):** `useAnnotation` owned ở shell; overlay truyền vào slot `VideoStage.overlay` — **2 chế độ loại trừ nhau:** đang vẽ (`annotation.active`) = canvas editable + toolbar; xem (`viewAnno`) = canvas read-only khi paused. Click chip **"Hình vẽ"** trên comment → `onViewAnnotation` (seek + pause + hiện hình read-only, ghim theo frame; tự ẩn khi phát/đổi version/bắt đầu vẽ mới). `TimelineMarkers` vẽ **thanh khoảng** dưới chấm cho range comment (nội bộ vàng / resolved lục / công khai chàm).
  - **pointer-events:** container overlay luôn `pointer-events-none`; chỉ `<svg>` editable bắt sự kiện → click-to-play dưới video vẫn chạy ở chế độ xem. Rect chuẩn hoá w/h dương; pen thin theo `PEN_MIN_STEP` + cap 2000 điểm; bỏ stroke degenerate.
  - Verify: `tsc --noEmit` + `next build --webpack` **xanh**. Player session-gated + cần Mux/R2 thật → verify bằng tsc+build (đồng nhất DoD P2/P3/P4.1-3); test tay pointer/vẽ chờ checkpoint.
  - **Review đối kháng 3-lens (geometry / react-state / spec-security) × skeptic (9 agent): 5/5 CONFIRMED → fixed hết (commit a380979):** (1)+(2) **[med] chế độ vẽ KHÔNG loại trừ với phát video** — overlay editable thiếu guard `!isPlaying` (khác nhánh read-only), Space/▶ vẫn phát khi đang vẽ → nét vẽ commit lệch frame nó được gắn → **fix: khoá frame khi vẽ (keydown chặn Space/step lúc `annotation.active` + effect re-pause nút ▶); tách effect clear `viewAnno`** để transition `active` true→false không xoá nhầm qua `isPlaying` cũ (stale, vì `ctlPause()` chỉ pause DOM, `isPlaying` flip async); (3)+(4) **[med] comment/annotation/range ở frame CUỐI bị server 400** (`startFrame/endFrame == totalFrames`, frame 0-indexed) — composer không biết totalFrames → **fix: truyền `durationMs` xuống composer, clamp frame gửi+hiển thị về `totalFrames-1` (dùng `frameCount`)**; (5) **[low] rect kéo dọc-mép trục** → w=0/h=0 lưu+đếm nhưng render vô hình → **fix: `buildShape` loại rect degenerate theo TỪNG trục**. tsc+build xanh lại. **CÒN LẠI P4: chỉ còn P4.5** (emoji picker + đính ảnh ≤6/≤10MB + lightbox + nút "Mở review"/đếm "chưa xử lý" ở drawer task); panel sort/filter/search [S] có thể trượt P6.
- ✅ **P4.5 — Emoji picker + đính ảnh + lightbox + "Mở review" ĐÃ LÀM (2026-07-06)** (nội bộ; backend attachment claim-by-key + reactions đã có từ P4.1 → chủ yếu frontend; **1 sửa read route additive**). **P4 KHÉP LẠI.**
  - **File mới:** `comment-attachments.ts` (client: `uploadCommentImage` = initiate presign → PUT thẳng R2 **kèm Content-Type khớp chữ ký** (r2 presignPutObject ký ContentType), đọc kích thước ảnh best-effort; `validateImageFile` image/* + ≤10MB); `EmojiPicker.tsx` (bộ emoji tuyển, 0 dep, 3 nhóm).
  - **CommentComposer (P4.5):** nút **emoji** → chèn tại con trỏ (`applyBody` giữ side-effect "ký tự đầu pause+freeze frame" + khôi phục caret); nút **đính ảnh** → input ẩn (image/*, multiple), cap ≤6 & ≤10MB client, thumbnail pending (spinner/errored) + nút xoá (abort upload + revoke objectURL), **chặn Gửi khi còn ảnh đang tải**, gom `attachments` (chỉ ảnh `done`) vào `createComment`; **cho phép comment CHỈ-ảnh** (body optional). Revoke objectURL khi xoá/gửi/unmount.
  - **CommentItem lightbox:** thumbnail → overlay `fixed inset-0 z-[100]`; keydown **capture-phase** trên window cho Esc/←/→ + `stopPropagation` để phím player (Space/step) KHÔNG bắn cùng; prev/next + đếm; click nền/ảnh đóng/không đóng đúng.
  - **task-assets read route (additive):** thêm `workspaceId` (client dựng link player) + `unresolvedCommentCount` (đếm top-level chưa-resolve chưa-xoá trên current version, 1 groupBy); **access vẫn re-check từ workspace resolved**, không trường tiền.
  - **TaskReviewUploadSection:** card ready thêm nút **"Mở review"** + poster click → `window.location.assign(/{workspaceId}/admin/team/asset/{assetId})` + chip **"N chưa xử lý"** (amber). Guard `workspaceId` rỗng.
  - **CSP đủ sẵn** (không sửa): `img-src` có `blob:` (thumbnail preview) + `'self'` (route `/raw` 302→R2); `connect-src` có `*.r2.cloudflarestorage.com` (PUT ảnh). Verify: `tsc --noEmit` + `next build --webpack` **xanh**.
  - **Review đối kháng 3-lens (attach-flow / react-state / spec-security) × skeptic (8 agent): 2/2 CONFIRMED [med] → fixed hết (commit sau aa9344f):** cả hai là **mất ảnh đính kèm âm thầm**: (1) ảnh upload LỖI + có body text → `canSend` chỉ chặn khi `uploading` (không chặn `error`) → gửi comment text-only, ảnh lỗi bị bỏ + xoá sạch không cảnh báo; (2) thêm ảnh TRONG lúc `await createComment` (nút đính kèm không disable khi submit) → success branch xoá `attachments` theo closure CŨ → ảnh mới biến mất + PUT không abort (rác R2) + objectURL rò. **Fix: (a) `canSend` chặn khi CÒN BẤT KỲ ảnh nào chưa `done` (uploading HOẶC error) — buộc retry/xoá trước; (b) thumbnail lỗi thành nút RETRY (lưu `file` trong record → `startUpload` dùng lại); (c) `addFiles` guard `if (submitting) return` + nút đính kèm disable khi submit; (d) revoke objectURL lúc gửi dùng `attachRef.current` (mới nhất) thay closure.** tsc+build xanh lại. **P4 (P4.1→P4.5) HOÀN TẤT. CÒN LẠI module: P5 (trang share khách `/r/[slug]`) + P6 (mention/notification fan-out + janitor purge attachment/version).**
- 🚨 **SỰ CỐ VẬN HÀNH (2026-07-06) — video kẹt "Phiên bản đang được xử lý…" vô hạn. ROOT CAUSE = Inngest chưa sync + CÁCH PHÒNG NGỪA.**
  - **Triệu chứng:** upload video review xong → card kẹt PROCESSING mãi, không bao giờ READY, không phát được (kể cả file 68MB nhẹ). Panel bình luận vẫn chạy.
  - **Root cause:** app Inngest chỉ được **sync (register) MỘT LẦN ở P0** (khi đó chỉ có 2 function: `review-mux-webhook`, `review-janitor`). Function **`review-process-upload`** (nhận `review/upload.completed` → tạo Mux asset) thêm ở **P1** nhưng app KHÔNG được sync lại → Inngest cloud không biết nó tồn tại → event bắn đi rơi vào khoảng không (0 function subscribe) → Mux asset không bao giờ tạo → không có webhook `video.asset.ready` → version kẹt PROCESSING. Bằng chứng: Inngest events API cho thấy `review/upload.completed` nhận lúc up nhưng `/runs` = `[]` (0 run). Không phải Mux, không phải R2, không phải dung lượng.
  - **Vì sao janitor không tự chữa:** sweep (c) reconcile-processing chỉ đụng version `updatedAt < now−20phút` (PROCESSING_GRACE); và nó chỉ **re-bắn cùng event** `upload.completed` — nếu app vẫn chưa sync thì event vẫn 0 run. Janitor KHÔNG sync app.
  - **FIX (2 lệnh production, KHÔNG sửa code):** (1) **`curl -X PUT https://hustlytasker.xyz/api/inngest`** → `{"modified":true}` + 3 audit-log `function.updated` (cloud học đủ 3 function). Từ đây MỌI upload mới chạy bình thường. (2) Cứu version đang kẹt: re-send event trực tiếp `POST https://inn.gs/e/$INNGEST_EVENT_KEY` body `{"name":"review/upload.completed","data":{"versionId":"…"}}` (idempotent — reviewProcessUpload re-check muxAssetId+PROCESSING). Đã verify chuỗi chạy full: upload.completed→reviewProcessUpload Completed (tạo Mux asset)→webhook created+ready→reviewMuxWebhook Completed `{processed:true}`→version READY. **Lưu ý:** version vừa up còn trong grace 20′ nên `GET /api/cron/review-janitor` (Bearer CRON_SECRET) KHÔNG cuốn nó — phải re-send tay hoặc đợi qua grace.
  - **PHÒNG NGỪA (QUAN TRỌNG cho P6 + mọi lần thêm Inngest function):** Inngest cloud CHỈ biết function qua sync. Nên: (a) **cài Inngest↔Vercel integration** trên dashboard Inngest → tự sync mỗi lần deploy (khỏi PUT tay) — cách bền nhất; HOẶC (b) sau mỗi deploy có thêm/đổi Inngest function, **PUT `/api/inngest`** thủ công. Nhớ: `serve({functions: reviewFunctions})` chỉ khai báo phía app; deploy code KHÔNG tự đăng ký với cloud. Script chẩn đoán read-only: `scripts/probe-stuck-processing.ts` (soi version/webhook/activity + hỏi Mux trực tiếp).
- 🚨 **SỰ CỐ #2 (2026-07-06) — player "đóng băng" ở frame 0 sau khi READY. ROOT CAUSE = CSP thiếu host CDN của Mux.**
  - **Triệu chứng:** video READY (poster nét, hover-scrub chạy), mở player thấy `00:00:00:00 / 00:00:00:00`, không spinner/không lỗi, Play/Space/click vô tác dụng; comment ghim 00:00:00 (playhead đứng ở 0).
  - **Chẩn đoán bằng bằng chứng:** (a) bóc frame từ screen-recording của user (ffmpeg qua imageio-ffmpeg) → thấy duration=0 + ready (không spinner) → manifest parse OK nhưng media không nạp; (b) `scripts/probe-signed-playback.ts` mint token thật + fetch chuỗi HLS → master trên `stream.mux.com` 200, **NHƯNG rendition trả về trên `manifest-oci-*.fastly.mux.com` + segment trên `chunk-oci-*.fastly.mux.com`** (Mux CDN steering, URL tuyệt đối có chữ ký riêng).
  - **Root cause:** CSP `connect-src` chỉ allow `stream.mux.com` + `image.mux.com` → hls.js tải master OK (→ MANIFEST_PARSED → ready=true, tắt spinner) rồi bị **trình duyệt chặn CSP** khi tải rendition/segment trên `*.fastly.mux.com` → duration=0, không phát được. Giả định P1.8 "chỉ cần stream.mux.com" SAI so với hành vi Mux thật.
  - **Fix (next.config.ts, cả 2 nhánh CSP):** `connect-src`/`media-src`/`img-src` → **`https://*.mux.com`** (CSP wildcard phủ MỌI tầng subdomain, gồm `a.b.mux.com`). media-src cũng cần vì Safari native-HLS tải segment qua media-src. **+ Hardening useHlsPlayer:** budget re-mint 403 trước reset ở MANIFEST_PARSED → dạng lỗi "master OK / rendition fail" tạo vòng re-mint vô hạn → chuyển reset sang `FRAG_BUFFERED` (media thật sự chảy).
  - **Lưu ý deploy:** đây là fix CODE → cần merge main mới có hiệu lực trên prod. Verify sau deploy: `curl -sI https://hustlytasker.xyz | grep -i content-security` phải thấy `https://*.mux.com`; rồi mở player bấm Play. Bài học: **test playback với URL playlist THẬT Mux trả về, đừng tin doc chỉ có stream.mux.com.**
- 🔧 **SỰ CỐ #3 (2026-07-06) — playback mờ vài giây đầu + khung hình co nhỏ rồi "giật" phóng to. ROOT CAUSE = ABR mặc định của hls.js + element `<video>` size theo rendition.**
  - **Triệu chứng:** sau khi fix CSP, video phát được nhưng: (1) 3–5s đầu mờ/vỡ nét rồi mới nét; (2) khung hình lúc đầu nhỏ, sau vài giây "giật" phóng to đầy stage. File gốc không sao — thuần hành vi player/stream.
  - **Root cause kép:** (1) hls.js mặc định khởi phát ở **rendition thấp nhất** rồi ABR leo dần — sai cho tool duyệt bản dựng (giây đầu là chỗ reviewer soi kỹ nhất); (2) `<video className="max-h-full max-w-full">` → kích thước hiển thị **bám kích thước nội tại của rendition đang phát** → ABR switch 480p→1080p làm cả element "nhảy" size.
  - **Fix (commit sau fdee8b9):** (1) `useHlsPlayer`: `autoStartLoad:false` + trong MANIFEST_PARSED set `hls.startLevel = levels.length-1` rồi `hls.startLoad()` — **khởi phát ở rendition CAO NHẤT** (pattern chính thức của hls.js docs; ABR vẫn bật sau đó, menu chất lượng vẫn Auto/pin được); (2) `VideoStage`: video → `h-full w-full object-contain` — **kích thước hiển thị bất biến**, letterbox bên trong element, khớp đúng phép `containedBox` của annotation overlay.
  - **Verify:** tsc+build xanh + **skeptic agent trace tận source hls.js 1.6.16** (node_modules): 6 nghi vấn REFUTED (refresh-403 re-fires MANIFEST_PARSED → startLoad lại ✓; manifest lỗi vẫn ra error path ✓; Safari native không đụng ✓; startLevel không đụng manualLevelIndex → menu vẫn "Tự động" ✓; levels sort thấp→cao nên length-1 = top ✓; deep-link seek trước fragment vẫn được UA giữ làm start position ✓) + **1 REAL đã bịt**: element phủ cả thanh letterbox → click thanh đen khi ĐANG VẼ kích play làm frame trôi khỏi frame ghim → thêm `clickToggleDisabled={annotation.active}` cho VideoStage + backstop draw-mode giờ `ctlPause()` **kèm `ctlSeek(annotation.frame)`** (bịt luôn lỗ nút ▶ cũ). Hạn chế biết trước: iPhone Safari native-HLS vẫn ABR kiểu Apple (không pin start-level được) — chấp nhận.
- **P5 (P5.1→P5.5) — Share link + guest review + task sync (commits 2f094f8, 34fe082, f5d2fc7, 0edce0e, 63dda55). Thay thế frame.io end-to-end từ đây.**
  - **P5.1 backend lõi:** `share-auth.ts` — `resolveShare` đúng chuỗi gate contract (404 SHARE_NOT_FOUND → 410 SHARE_REVOKED → 410 SHARE_EXPIRED → 401 SHARE_PASSWORD_REQUIRED; message not_found ≡ revoked chống dò slug); cookie unlock `rv_unlock_{slug}` (JWT HS256, `REVIEW_COOKIE_SECRET`, TTL min(24h, expiry)); guest identity = **GuestSession + token 32-byte raw trong HttpOnly cookie `rv_guest_{slug}`, DB chỉ giữ sha256** (đúng schema P0 — KHÔNG phải JWT chứa name/email như KIEN-TRUC phác; DATA-MODEL/schema thắng). Cookie path `/` (không phải `/r/{slug}` như KIEN-TRUC — vì API nằm ở `/api/r/*`). `shares.ts` staff CRUD (nanoid(12), bcrypt 4–72, rowVersion lock, USER thấy own+assigned-task, ADMIN thấy hết, guestEmail trong activity CHỈ admin). `share-guest.ts` — scope resolver (asset trực tiếp + đệ quy folder theo materialized path, soft-deleted biến mất per-request, cap 100 asset/Reel), `assertVersionInShare` 404 đồng nhất + head-only khi `showAllVersions=false`; **đếm comment public LIVE, không dùng `commentCount` denormalized (chứa internal → rò delta)**.
  - **P5.2 guest comments:** `share-comments.ts` — **`isInternal=false` nằm trong SQL WHERE ở MỌI đường** (list, since-delta deletedIds, parent reply, reactions, attachment raw); write FORCE isInternal=false + guestSessionId + guestName snapshot + shareLinkId; reply parent internal → 404 y hệt không tồn tại; author member serialize **không kèm user id**; attachment guest key `review-attach/guest/{sessionId}/…`; route raw riêng `/api/r/{slug}/comment-attachments/{id}/raw` (route nội bộ member-authed không dùng được cho khách).
  - **P5.4 decision + sync:** `share-decision.ts` — transaction: flip `reviewState` (optimistic theo state quan sát; idempotent double-click; **PRD thắng KIEN-TRUC: khách đổi ý được APPROVED⇄CHANGES_REQUESTED, latest wins**) + set `asset.statusId` theo `REVIEW_STATUS_MAP` (map hỏng → record-only + log, FR-D02 fallback) + note→comment public không timecode + activity `review.approved|review.changes_requested`; xong emit **`review/decision.recorded`**. Inngest `review-share-decision` (id mới — **ĐÃ đăng ký vào `reviewFunctions`; cần Inngest sync sau deploy — Vercel integration tự làm**): supersede-check (state hiện tại phải khớp decision, latest-wins), request_changes → `syncTaskOnChangesRequested` (ghi task TRỰC TIẾP vì updateTaskStatus cần session mà guest không có: status='Revision' + deadline=null theo STATUS_REQUIRES_NULL_DEADLINE + version increment, thua race thì bỏ qua sạch) + AuditLog **`task.client_changes_requested` / `task.client_approved`** (2 action CÓ SẴN trong EVENT_LABELS của feed task — "khách yêu cầu chỉnh sửa"/"khách đã duyệt"; giả định spec "string sẵn có" là ĐÂY, không phải string tự do) + notify assignee+manager (`VIDEO_CHANGES_REQUESTED`/`VIDEO_REVIEW_APPROVED` enum sẵn có). Banner "chuyển Hoàn tất?" TÁI DÙNG banner P3 (kích theo `asset.statusId === 'Hoàn tất'` mà decision route đã set).
  - **P5.3 trang share `/r/{slug}`:** KHÔNG fork 1.600 dòng UI comment — thêm **`PlayerEnv` context** (player-env.tsx): internal (mặc định, VN, /api/review/*, đủ quyền — player nội bộ không đổi hành vi) vs guest (EN, /api/r/{slug}/*, không internal-toggle/mention/resolve; edit/delete = comment của chính session qua mineIds). `player-l10n.ts` VN+EN (EN verbatim UI-UX §6.5). `GuestReviewApp` — header decision (Approve/Request changes + modal + banner), Reel prev/next, download gated theo approve, gate processing, banner "A new version is available — Reload" (không auto reload), **identity modal chặn WRITE đầu tiên** (promise-queue resume đúng hành động đang dở), events link_opened/asset_viewed (debounce 30' server qua cookie `rv_t_{slug}`), poll content 5s. Route ảnh riêng `view-url` (IMAGE-only, KHÔNG gate allowDownload — xem ảnh là playback; video vẫn sau gate download).
  - **P5.5 UI quản lý:** ShareLinkModal (§7.2), trang `/admin/team/shares` (§7.3 rút gọn — chưa có "Xem hoạt động" drawer + search theo tên [S]), context menu "Tạo link chia sẻ" LIVE (multi-select qua acting selection), nút Share2 toolbar → trang shares, drawer task "Copy link khách" (get-or-create 1 click + clipboard).
  - **Deviation/deferred (ghi rõ):** (1) event ghi qua route `/events` duy nhất — GET content KHÔNG side-effect (API-SPEC §5.5.2 muốn GET tự ghi; 1 writer đỡ race); (2) viewCount increment trực tiếp trong events route (throttle 30' cookie) thay vì Inngest debounce 30s batch (DATA-MODEL §9) — traffic hiện tại chưa cần; (3) GuestShareRes trả `allowDownload`+`downloadOnlyWhenApproved` thô (client tính per-version) thay vì AND sẵn — Reel nhiều asset trạng thái khác nhau; (4) toolbar "chế độ chọn để share" banner-mode → P6 (context-menu multi-select đã phủ nhu cầu); "Thêm vào link chia sẻ" [S], un-revoke có sẵn nhưng filter/search bảng [S], sidebar "LINK CHIA SẺ recent 5" → P6; (5) chuỗi "Khách hàng yêu cầu chỉnh sửa" KHÔNG tồn tại sẵn trong app như spec đoán — dùng AuditLog action `task.client_changes_requested` (label feed "khách yêu cầu chỉnh sửa") làm tương đương; (6) banner approve trang share không kèm {date} (state đọc từ reviewState, không lưu decidedAt) — copy "You approved this video."
  - **ENV MỚI CẦN SET TRÊN VERCEL TRƯỚC KHI DÙNG P5: `REVIEW_COOKIE_SECRET`** (32+ bytes random, đã có sẵn dòng trong .env.example từ P0). Thiếu → unlock/identity/comment guest 500.
  - **REVIEW ĐỐI KHÁNG P5 (4 finder × verify skeptic = 24 agent): 16/16 CONFIRMED, 0 refuted. Đã SỬA 13, DEFER-có-tài-liệu 3. Commit sau 63dda55.**
    - **[BLOCKER] Guest quyết định không mở modal danh tính → Approve/Request-changes chết ở 401 loop.** submitDecision gọi thẳng api không `await ensureIdentity()`; khách mới mở link bấm Approve → 401 vĩnh viễn (share allowComments=false thì KHÔNG có đường nào tạo identity → tính năng lõi FR-F03 vô dụng). **Fix:** truyền `ensureIdentity` vào GuestStage + `await` trước khi submit; đóng modal đúng path lỗi.
    - **[HIGH] deleteShare gate theo GLOBAL JWT role thay vì workspace-admin.** `requireReviewAccess({admin:true})` kiểm `User.role` toàn cục → (a) global-ADMIN chỉ là MEMBER workspace VẪN hard-delete được link người khác (leo thang, mạnh hơn revoke); (b) workspace OWNER role USER KHÔNG xóa được link mình. **Fix:** dùng `access.isAdmin` (workspace-scoped) như revoke/update.
    - **[HIGH] Note "Request changes" mất âm thầm** khi version đã CHANGES_REQUESTED (bấm lần 2 với note mới) hoặc thua race → early-return 200 trước khi tạo comment. **Fix:** tách `persistDecisionNote` (tx riêng) gọi cả ở nhánh idempotent + lost-race — note là NỘI DUNG MỚI, độc lập với flip.
    - **[HIGH] Side-effect quyết định mất nếu `inngest.send` fail sau commit** (guest nhận 500 dù đã commit; retry rơi vào idempotent 200 không re-send → task không auto-Revision, không audit, không notify). **Fix (một phần):** try/catch quanh send + log; guest nhận 200 đúng (quyết định đã commit). RESIDUAL: mất side-effect khi Inngest chết hẳn — outbox thật để P6 (đã ghi).
    - **[HIGH] Version pinning vỡ khi showAllVersions=false:** upload mới → poll bỏ head cũ → GuestReviewApp fallback hot-swap sang v2 (player reset frame 0, comment đang gõ post NHẦM version), banner "Reload" không bao giờ hiện. **Fix:** snapshot version đang ghim (`versionSnap`), KHÔNG fallback-swap; `newHead` tính theo id ghim; key CommentsPanel theo versionId (reset composer khi đổi version).
    - **[HIGH] ShareLinkModal edit nói dối expiry + ghi đè now+30d mỗi lần lưu:** preset init '30d' cho MỌI expiry sẵn có → lưu đổi tên/toggle khác cũng đẩy hạn thành +30 ngày. **Fix:** thêm preset 'keep' (mặc định edit-mode, nhãn hết hạn thật); `expiresAtValue()` trả undefined khi 'keep' → PATCH không đụng expiresAt.
    - **[MED] Unlock JWT không bind mật khẩu:** đổi mật khẩu (nghi lộ) KHÔNG cắt session đã unlock (còn tới 24h). **Fix:** thêm claim `pv = sha256(passwordHash)[:16]` vào JWT; verify so khớp hash hiện tại → đổi/bỏ mật khẩu vô hiệu mọi cookie unlock cũ.
    - **[MED] getShareDetail + getOrCreatePrimaryShareForAsset thiếu scope creator/assignee:** member ngoài scope đọc được guest-activity (tên khách/lượt xem) của link đồng nghiệp; id leak qua route get-or-create. **Fix:** `assertShareInScope` (creator hoặc task assigned) 404 anti-enum cho getShareDetail; non-admin chỉ reuse link own/assigned trong get-or-create (else tạo mới).
    - **[MED] Quyết định trên version KHÔNG-head (showAllVersions=true) ghi đè status card + kéo task sai.** **Fix:** `isHead = asset.currentVersionId === version.id`; chỉ ghi `asset.statusId` + emit DECISION_RECORDED khi isHead — quyết định version cũ chỉ flip reviewState của chính nó.
    - **[MED] Head mới READY không xóa approval cũ của khách** → banner "chuyển Hoàn tất" ma, 1 click hoàn tất task với cut chưa duyệt. **Fix:** applyMuxReady (video) + upload-service (ảnh) `updateMany({where:{statusId:approved}}, {statusId:null})` khi set currentVersionId — chỉ xóa giá trị approved, không đụng status khác (FR-A04 AC2).
    - **[MED] Supersede guard không thấy new-version/staff-supersession** → retry trễ hàng giờ kéo task về Revision + null deadline. **Fix:** load-state đọc thêm `isHead`; skip khi `!isHead` (decided version cũ giữ reviewState riêng nên check-state đơn thuần mù new head).
    - **[MED] Composer khách hiện trên share tắt-comment (khi có comment public cũ)** → gõ/đính ảnh đi qua modal danh tính rồi 403. **Fix:** CommentsPanel `readOnly` (từ GuestReviewApp khi !allowComments) ẩn composer + nút Reply; giữ đọc/edit-own/react.
    - **[LOW] syncTaskOnChangesRequested flip task Đã-hủy/archived → Revision** (state bất khả). **Fix:** select `isArchived`, skip khi archived/Đã hủy + `isArchived:false` trong where.
    - **[LOW] Notification nói "đã tự chuyển Revision" cả khi sync bị skip/thua race.** **Fix:** bắt `res.applied`, đổi body thành "khách yêu cầu chỉnh sửa, kiểm tra trạng thái task" khi !applied.
    - **DEFER (ghi rõ, không chặn P5):** (1) **[MED] view-url ảnh phục vụ file gốc full-res, bỏ qua allowDownload/downloadOnlyWhenApproved** — xem ảnh = phục vụ bytes; fix thật cần pipeline tạo rendition thu nhỏ (ảnh là asset phụ, module chủ yếu video) → P6; hiện allowDownload chỉ ẩn NÚT tải, không chặn lưu-ảnh-đang-xem (đã ghi). (2) **[LOW] revoke không cắt ngay stream** — Mux token đã mint sống tới 6h (spec cho 6–12h; tradeoff đã biết); giảm TTL + re-mint chủ động → P6. (3) **[LOW] notify-staff double-notify khi step retry giữa chừng** (cần lỗi DB transient); dedupe per-recipient → P6.

## P6 — Hoàn thiện: notifications + activity log + trash purge + status history (2026-07-07)

**P6 KHÔNG schema mới** (thuần additive code; purge = xoá-cascade thuần, ReviewActivity không FK nên audit trail sống sót). 4 sub-phase:

- **P6.1 Notifications (FR-G02, commit dd595cc):** `lib/review/notify.ts` — `notifyReview` (loại actor, `taskId` cho bell + `metadata.url` deep-link player cho web-push, dedupe, không throw) + `resolveTaskRecipients` (assignee + profile OWNER/ADMIN) + `reviewPlayerUrl`. Wire: version ready/failed → uploader (applyMuxReady/applyMuxErrored); guest comment public → assignee+admin (createGuestComment); @mention trong review comment nội bộ → người được nhắc (createComment — validate uuid client gửi PHẢI là profile staff thật trước khi notify, loại author, chạy cả comment INTERNAL vì khách không thấy internal). Tái dùng `createAndBroadcastNotifications` (bell+realtime+web-push); type `VIDEO_VERSION_UPLOADED`/`VIDEO_COMMENT_NEW` (định nghĩa sẵn, giờ mới fire).
- **P6.2 Trash purge + Delete forever (FR-B13, commit 01482bc):** `lib/review/purge.ts` — teardown ref-count (KHÔNG xoá Mux asset/R2 object còn được version LIVE tham chiếu qua `muxPlaybackId`/`r2Key` — copy-on-reference P2.5 chia sẻ), external-trước-rows (idempotent retry), thứ tự cascade (asset xoá cascade version/comment/attachment; folder sâu-nhất-trước, skip nếu còn con LIVE = GAP-3); attachment R2 xoá TRƯỚC khi cascade drop row. `purgeExpiredTrash(25)` thành **sweep thứ 5 trong reviewJanitor** → chạy nhờ cron 03:00 sẵn có, KHÔNG thêm Inngest function (né gotcha sync). `manualPurgeItems`+`purgeItemsAuthorized` (ADMIN workspace-scoped, không global role) → `POST /api/review/trash/purge` + `apiPurgeItems`. TeamTrash: nút "Xóa vĩnh viễn" per-row + bulk (ADMIN) + confirm modal (copy verbatim). NEXT_PUBLIC deep-link.
- **P6.3 Activity → task feed + Nhật ký hoạt động (FR-G01/G03, commit 7abc480):** `lib/review/feed-audit.ts` — `auditReviewFeed` dual-write AuditLog (song song ReviewActivity, đúng mẫu P5.4 dùng cho decision) → lên CẢ feed drawer task (getTaskActivityFeed đọc AuditLog action∈EVENT_LABELS, targetType='Task') VÀ trang admin. Wire: version ready · guest comment (actor null → "Khách hàng") · share created/revoked. AuditAction union += `video.version_ready/guest_commented/share_created/share_revoked`; EVENT_LABELS + ACTION_LABELS (nhóm "Video review"); row decision cũ giờ cũng có nhãn.
- **P6.4 FR-G04 status history [S] (commit a625454):** `lib/review/status-history.ts` — `getAssetStatusHistory` đọc ReviewActivity (version.ready/review.approved/review.changes_requested/asset.status_changed/task.completed_from_asset) làm VIEW ReviewStatusHistory (không bảng mới); `GET /api/review/assets/[id]/status-history` (member-only); InfoTab render "Lịch sử trạng thái", actor = tên member / "Khách: {name}" / "Hệ thống". FR-B12 empty-state đã đủ từ P2-P5; các mục "DỪNG LẠI" chỉ nằm trong comment, không có UI.

**GOTCHA vận hành nhắc lại:** P6 KHÔNG thêm Inngest function (purge là sweep trong reviewJanitor sẵn có) → không cần bước sync riêng, chỉ cần merge PR để deploy. (P5 mới là phase thêm function `review-share-decision` cần sync.)

- **REVIEW ĐỐI KHÁNG P6 (4 lens × verify, 9 agent): 5/5 CONFIRMED, 0 refuted → sửa hết (commit sau a625454):**
  - **[BLOCKER] purge ref-count chỉ đếm sibling LIVE** → xoá 1 copy sẽ huỷ media Mux/R2 CHIA SẺ của một copy TRASHED-còn-khôi-phục-được → mất dữ liệu vĩnh viễn khi restore. **Fix:** ref-count đếm MỌI sibling (live HOẶC trashed) cùng r2Key/muxPlaybackId — chỉ holder CUỐI CÙNG mới teardown external.
  - **[HIGH] BigInt trong purge stats vỡ serialize** — `bytesFreed: bigint` qua ranh giới Inngest step + logger JSON.stringify → throw "Do not know how to serialize a BigInt" → **crash sweep purge lúc chạy**. **Fix:** purgeExpiredTrash trả `bytesFreed` dạng string (PurgeSummary).
  - **[MED] Purge original rò Mux asset vĩnh viễn** — copy chia sẻ muxPlaybackId nhưng muxAssetId=null (@unique); xoá original (giữ muxAssetId) skip-mux đúng, nhưng sau này copy purge (muxAssetId=null) → Mux asset mồ côi, tính tiền mãi. **Fix:** re-home muxAssetId sang sibling sống trước khi xoá row original (1 tx: clear ở đây, set ở kia).
  - **[MED] Un-revoke bị gán nhãn 'video.share_revoked'** → feed/Nhật ký hiển thị "thu hồi" khi thực ra "mở lại". **Fix:** action riêng `video.share_unrevoked` + nhãn EVENT_LABELS/ACTION_LABELS.
  - **[LOW] Cron xoá folder gặp FK Restrict (nuốt lỗi)** khi cap 25-asset để sót con trashed dưới folder quá hạn. **Fix:** `folderHasDescendantRow` đếm MỌI con (live+trashed, trừ chính nó) — hoãn folder sang đêm sau.
  - tsc+build xanh. Purge session-less + cần Mux/R2 thật → verify bằng tsc+build+review đối kháng (như P2-P5).

## P3-B — auto-transition F7/F8/F9/F10-flip + guest A6 (review-fixes, 2026-07-07)

**Nối 4 auto-transition trên nền F2 (P3-A). KHÔNG schema mới, KHÔNG Inngest function mới (thêm STEP vào `applyMuxReady`), KHÔNG bật FSM enforcement. File chạm:** `task-sync.ts` (lõi), `inngest.ts` (F7 hook + guest notify body), `notify.ts` (manager resolver), `status-map.ts` (6 key config), `versions.ts`+`team-actions.ts` (asset payload +taskId/taskStatus/assigneeId), `activity.ts` (3 type), `ReviewPlayerShell.tsx`+`ReviewFlowActions.tsx` (nút), 3 route mới `assets/[id]/{feedback-done,confirm-fix,approve-send}`.

- **Kiến trúc write:** 2 đường. (a) **Không session** (F7 Inngest, guest A6): `syncTaskFromReviewEvent(taskId, ws, target)` — direct `prisma.task.updateMany` optimistic (pattern `syncTaskOnChangesRequested`), predecessor-guard CHỈ khi `target ∈ STATUS_TRANSITIONS` (video status); target legacy như `'Revision'` (không phải key) flip vô điều kiện = giữ hành vi cũ (K6). (b) **Có session** (F8/F9/F10 route): DELEGATE `updateTaskStatus` (tái dùng RBAC workspace-admin-OR-assignee + optimistic version + StatusHistory + revalidatePath) + role-guard riêng (`access.isAdmin` cho F8/F10; assignee-or-admin cho F9) + `canAutoTransition` guard trước khi gọi (updateTaskStatus KHÔNG enforce FSM, R10).
- **⚠️ DEVIATION notification (§5 bảo "template VIDEO_* staff mới"):** KHÔNG thêm template VIDEO_*. Lý do: 4 enum `VIDEO_*` đang được dùng cho việc khác (VIDEO_VERSION_UPLOADED = uploader confirm in-app; VIDEO_REVIEW_APPROVED/CHANGES_REQUESTED = guest-decision staff) và hiện **email-less** (pickTemplate→null); thêm template sẽ BẬT email cho các use cũ đó (over-email editor + staff). Thay vào đó **tái dùng type `TASK_STATUS_CHANGED`** (đã có template `taskStatusChanged` render từ meta.oldStatus/newStatus): editor nhận email/in-app qua `updateTaskStatus`'s generic `notifyTaskStatusChanged` (F8/F10); manager nhận qua `notifyManagerOfReviewFlip` (type TASK_STATUS_CHANGED → gửi tới manager recipients, KHÔNG chồng lên editor). **KHÔNG double-email** (R6): F7 manager once (khóa bởi atomic READY flip); F8 editor once (generic); F9 manager once (actor=editor→generic self-skip nên chỉ manager helper bắn); F10 editor once (generic).
- **E2b (fallback manager) = workspace-level (đã chốt):** `resolveManagerRecipients` = `Task.assignedById` nếu có, else mọi OWNER/ADMIN của **workspace** (`WorkspaceMember.role`), KHÔNG profile-level như `resolveTaskRecipients` cũ.
- **⚠️ DEVIATION F7 predecessor A4→A2:** hằng `STATUS_TRANSITIONS['Đã nộp video (nội bộ)'] = ['Đang thực hiện','Sửa frame','Gửi lại','Revision']` (SSOT §3.2, khớp DATA-MODEL §5.1 + TASK-BREAKDOWN P3-4) **KHÔNG gồm A4** — trong khi prose §3.3 nói predecessor "A1 (lần đầu) HOẶC A4 (vòng lặp upload version mới)". Theo HẰNG (SSOT). Hệ quả: editor upload version mới khi task đang A4 → task **giữ nguyên A4** (guard no-op), version vẫn thành head + uploader vẫn nhận in-app; manager KHÔNG bị bắn "chuyển A2" sai (chỉ bắn khi flip thật applied). Chấp nhận được (không mất dữ liệu); admin Duyệt từ A4 vẫn được (A4 là predecessor của A5).
- **A6→A7 (client round) nối luôn qua cùng nút F9 "confirm fix":** `confirmFixDone` chọn target theo current — A3→A4 (nội bộ) HOẶC A6→A7 (khách). Symmetric với F9, guard bằng canAutoTransition, **inert tới khi vòng khách chạy (P4 portal)**. Nút hiển thị nhãn "(nội bộ)"/"(khách)" theo target.
- **⚠️ Bẫy A6-demotion (đã bịt trước review):** guest "request changes" lần 2 khi task ĐÃ ở A6 → `canAutoTransition(A6,A6)=false` sẽ chọn 'Revision' → **DEMOTE A6→Revision**. `syncTaskOnChangesRequested` short-circuit `if (task.status === clientChangesRequested) return no-op` TRƯỚC khi chọn target. Test `test-auto-transition.ts` khóa bất biến này (A6 không phải predecessor của chính nó).
- **F8 confirm-on-leave:** 3 tầng — nút tường minh "Kết thúc feedback" (reliable) + `window.confirm` bọc `goBack` (OK=chốt&thoát, Cancel=thoát-chưa-chốt) + `beforeunload` best-effort (KHÔNG chạy được async flip lúc unload — chỉ cảnh báo native). Gate: admin + task ở A2 + có ≥1 comment. "Xác nhận"=chốt phiên (comment đã ghi ngay, không draft) — đúng §3.4.
- **UI gating cần task context:** `GET /api/review/assets/[id]/versions` (listVersions) giờ trả thêm `asset.{taskId,taskStatus,assigneeId}` (additive, /api/review/* nội bộ) để `ReviewFlowActions` derive nút bằng `canAutoTransition` client-safe (server re-guard mọi flip). Sau flip → `reloadAsset` re-fetch.
- **Bridge B5 (ShareLink + Task.clientReview='AWAITING') + email khách = P4** (BR-05/FR-10-portal), CHỈ chạy khi admin Duyệt (không lúc READY — né R5 lộ bản nội bộ). P3-B chỉ flip status + notify staff.
- **Guard money-safety (R1):** không đụng SALARY_* / meta salaryPending; test khóa "mọi auto-transition target là salaryPending". Cổng xanh: `tsc` + `test:status-meta` 26/26 + `test:portal-derive` + `test:auto-transition` mới + `build:mcp` + `next build` (3 route mới đăng ký).

**REVIEW ĐỐI KHÁNG P3-B (4 lens × verify high-effort): lens Money/RBAC CLEAN; 6 finding CONFIRMED ở 3 lens còn lại → SỬA 5, ghi-nhận 1:**
- **[MED] A7 cũng bị demote về 'Revision'** (guard already-A6 thiếu 1 node): guest "request changes" lần 2 khi task ở A7 'Đã sửa feedback (khách)' (editor đã xác nhận, chờ admin gửi lại) → A7 không phải predecessor của A6 → rơi xuống 'Revision' + null deadline, mất vòng đời khách. **Fix:** guard no-op cho CẢ A6 lẫn A7 (`clientFixDone`); test khóa "A7 → noop".
- **[MED] A4→A2 vòng re-upload nội bộ bị chặn:** editor sửa xong (A4) upload cut mới → guard chặn (A4 không trong predecessor của A2) → task kẹt A4, đường auto duy nhất là F10→khách (đẩy cut chưa duyệt ra ngoài). Mâu thuẫn §3.1/§3.3 (đều vẽ A4→A2). **Fix:** thêm A4 vào `STATUS_TRANSITIONS['Đã nộp video (nội bộ)']` (hoà giải hằng §3.2 với intent §3.1/§3.3); test đổi "A4 → A2 allowed".
- **[MED] `notifyManagerOfReviewFlip` rò unhandledRejection:** `resolveManagerRecipients` (prisma reads) nằm NGOÀI try/catch của `notifyReview`, cả 2 call-site `void` → DB lỗi transient → reject mồ côi sau khi request/step đã return. **Fix:** bọc TOÀN THÂN hàm trong try/catch + reviewLog (đúng hợp đồng "never throws").
- **[LOW] F9 admin-confirm double-notify editor:** nếu admin (≠assignee) bấm confirm-fix VÀ assignee nằm trong manager set (self-assign / là workspace-admin khi assignedById=null) → editor nhận 2 in-app + 2 email (generic + manager). **Fix:** thêm `alsoExcludeIds` — loại assignee (F9) / uploader (F7) khỏi manager recipients (họ đã nhận kênh riêng).
- **[LOW] `recordActivity` fail che notify + báo 500 sai:** confirmFixDone chạy delegateFlip (đã commit) → recordActivity (throw) → notify không tới, trả 500, retry bị guard chặn. **Fix:** `safeRecordActivity` best-effort (try/catch log) cho cả 3 service + dời notify F9 TRƯỚC audit.
- **[LOW — ghi nhận, không sửa] F7 task-flip kẹt A1 khi flip lỗi:** version đã READY, syncTaskFromReviewEvent throw → try/catch nuốt (không fail pipeline). Structurally at-most-once, không reconcile lại (janitor chỉ đụng PROCESSING). **Chấp nhận theo spec** (admin set tay); auto-recovery (sweep flip READY-version-task-còn-ở-predecessor) = cải tiến ngoài phạm vi P3-B. Cổng lại xanh sau fix: tsc + 3 test + next build.

## P4 — Gói khách: FR-11 guest email + BR-05 bridge (review-fixes, 2026-07-07)

**Migration DUY NHẤT của cả dự án** (additive-only, NFR5): 2 model + 1 cột. 6 sub-phase, mỗi cái 1 commit deploy-được: 1cda7cc (P4.1 schema + P4.2 OTP backend) · 0b7c0fe (P4.3 template + fan-out) · 480ea6d (P4.4+P4.5 bridge + E1) · c06ec00 (P4.6 UI).

- **P4.1 schema:** `GuestEmailVerification` (PIN 6 số: codeHash SHA-256, shareLinkId, attempts, expiresAt, consumedAt, requestIp), `GuestSubscription` (**key `(email,assetId)`** sống qua rotate slug; `unsubscribeToken @unique @default(cuid())`; `asset` relation onDelete Cascade), `GuestSession.emailVerifiedAt`, `ReviewAsset.subscriptions` back-relation. Dùng `@default(cuid())` cho 2 model mới (spec §1.6) — khác convention `uuid()` của review-module (ghi nhận, cả 2 hợp lệ). **`prisma validate` OK; regen `prisma generate --no-engine`** — repo dùng Neon driver-adapter nên KHÔNG cần query-engine binary lúc chạy; `--no-engine` né lỗi EPERM lock DLL trên Windows (2 process `mcp-server/dist/index.js` giữ `@prisma/client`; KHÔNG kill process không do mình tạo). **CHƯA `db push`** lên DB nào — test frosty-forest ở QA, prod chỉ khi merge main (additive an toàn).
- **P4.2 OTP (surface A = /r/[slug] guest, KHÔNG phải client-portal token):** tái dùng `otp.ts`; rate-limit qua **`limitDb` (Postgres/RateLimitBucket)** — KHÔNG Upstash (auth-only, fail-closed) KHÔNG in-memory (reset cold-start). `request-pin` **ANTI-ENUM: LUÔN 200** (rate-limit/invalid → body status 'cooldown'/'pin_sent', không 429) — route đầu tiên của guest surface phá vỡ pattern 429; 3 giới hạn (3/10' per email+share, 10/ngày/IP, 60s cooldown). `verify-pin` (5-attempt cap + 10' TTL + newest-unconsumed) có thể trả non-neutral (khách nhập code của chính mình). `assetInShare` guard chặn subscribe asset ngoài share. `unsubscribeGuestByToken` one-click RFC 8058 (`/api/r/unsubscribe` POST/GET) + `/r/unsubscribe` page (server-render unsubscribe).
- **P4.3 email:** `sendEmail` thêm optional `headers` (additive, backward-compat) cho List-Unsubscribe. 4 template EN (`guest-emails/`): T1 verify-pin (transactional, KHÔNG footer unsub), T2 new-version, T3 comment-reply, T4 status-update (bắt buộc footer unsub). `notifyGuestsOfAsset` direct Resend fan-out — per-recipient unsubscribe token + `List-Unsubscribe`/`List-Unsubscribe-Post: One-Click` header, slug mới nhất non-revoked, never throws.
- **P4.4+P4.5 bridge (R5-CRITICAL):** `approveInternalAndSendToClient` (P3-B) sau flip A5 chạy bridge **CHỈ tại đây (admin Duyệt), best-effort try/catch**: get-or-create ShareLink + `Task.clientReview='AWAITING'` (→ `deriveClientStatus` "Awaiting your review" + nút Approve portal) + `productLink=/r/{slug}` + **E1** `notifyGuestsOfAsset('version_sent')`. R5 guard: bridge KHÔNG bao giờ chạy ở Mux READY (F7 chỉ flip A2). **⚠️ RESIDUAL R5 để review đối kháng soi:** /r/ share hiện HEAD version — nếu editor upload cut nội bộ MỚI sau Duyệt, link cũ có thể lộ cut chưa duyệt (P5-era share design). Đang chờ review confirm.
- **⚠️ DEVIATION P4.4 — E2/E3 DEFER (ghi rõ):** chỉ wire **E1** (version_sent, giá trị lõi). **E2** (comment-reply digest 15') — digest cần schema marker HOẶC Inngest debounce function (đều vướng K8/NFR5); **E3** (status_update) — hook rộng vào `updateTaskStatus` (mọi status change toàn app) HOẶC mapping task→asset phức tạp, giá trị biên thấp hơn E1 + phần lớn status client-visible là hành động của chính khách. Cả 2 là follow-up additive, KHÔNG chặn trải nghiệm lõi "khách nhận video đã duyệt + email + duyệt được".
- **P4.6 UI:** `GuestNotifyControl` (bell trong header /r/) — tự fetch status → double-opt-in (email → PIN 6 số → subscribed); tắt qua link email (token), không expose in-app. Thread `slug` vào `GuestStage`.
- Cổng: `prisma validate` + `tsc` + `next build` (routes `/api/r/[slug]/notifications/*` + `/api/r/unsubscribe` + `/r/unsubscribe` đăng ký). KHÔNG đụng salary/mobile/FSM.

### P4 — Review đối kháng R5/bảo mật (workflow 4 lens) + VÁ trước khi đóng (2026-07-07)
Review đối kháng (4 agent: R5-leak / anti-enum-PIN / unsubscribe-token / bridge-idempotency) tìm **1 BLOCKER + 3 HIGH + 3 MEDIUM/LOW đã confirm** — sửa hết trong cùng phase, tsc+build xanh:
- **[BLOCKER] R5 — cut nội bộ MỚI lộ cho khách qua /r/ HEAD** (`task-sync.ts` + `inngest.ts`): sau bridge gửi v1, editor upload v2 → `applyMuxReady` set `currentVersionId=v2` vô điều kiện; task ở A5/A6 nên `clientReview` giữ 'AWAITING'; `buildGuestShareContent` phục vụ HEAD=v2 → khách xem cut CHƯA duyệt lại. ShareLink track stack (assetId) KHÔNG track versionId nên không pin được. **VÁ:** thêm `revokeClientExposureOnNewVersion(taskId,assetId,ws)` — khi HEAD mới về trên task đã-gửi-khách (`clientReview='AWAITING'`): **revoke các share active của asset + clear `clientReview`**; link khách chết tới khi admin **re-Duyệt** (bridge get-or-create share MỚI + re-email). Gọi ngay đầu nhánh applied của `applyMuxReady`, self-guard + never-throw. Vòng đầu A1→A2 và vòng nội bộ KHÔNG bị đụng.
- **[HIGH] request-pin enum oracle + force-subscribe** (`guest-subscribe.ts` + `request-pin/route.ts`): body `status` cũ (already_subscribed/already_verified/pin_sent) LỘ trạng thái sub của email tuỳ ý + nhánh already_verified auto-tạo sub cho email attacker gõ (không PIN/consent). **VÁ:** route **LUÔN trả neutral `{status:'pin_sent'}`** (kể cả rate-limit); skip-PIN/auto-subscribe **CHỈ khi `isOwnEmail`** (email == session guest) — email lạ luôn phải verify PIN từ inbox của chính nó. UI "đã bật" nay đọc từ endpoint status (session-gated), không từ response request-pin.
- **[HIGH] clientReview kẹt 'AWAITING'**: khách duyệt trên /r/ + admin confirm Hoàn tất → portal kẹt "Awaiting your review" mãi (chỉ token-portal actions từng ghi clientReview). **VÁ:** `confirmTaskHoanTat` settle `clientReview='APPROVED'` (chỉ task đang trong luồng khách, clientReview≠null); `syncTaskOnChangesRequested` (A6) set `clientReview='CHANGES'`.
- **[MEDIUM] bridge clobber `productLink`**: bridge ghi đè vô điều kiện link giao hàng staff nhập + gộp multi-asset. **VÁ:** chỉ set productLink khi rỗng HOẶC đã là link `/r/`.
- **[MEDIUM] unsubscribe mutate trên GET** (scanner/prefetch tự unsub): `/r/unsubscribe` page nay **KHÔNG mutate lúc render** — client `UnsubscribeConfirm` chỉ POST khi bấm nút; `/api/r/unsubscribe` GET **redirect (non-mutating)** về page, CHỈ POST mới ghi (RFC 8058 one-click = POST-only).
- **[LOW] verify-pin P2002 race**: double-submit PIN đúng đồng thời → upsert đụng unique → 500. **VÁ:** catch P2002 → coi như idempotent `{ok:true}`.
- Ghi đè residual note ở P4.4 (dòng trên): R5 residual **ĐÃ ĐÓNG** bằng revoke-on-new-version; anti-enum note ở P4.2 **ĐÃ THAY** bằng neutral-always + isOwnEmail gate.
