# PROGRESS — Mission Control (Giao diện 2)

Branch: `claude/mission-control` · **commit-only, KHÔNG push** · design = 31 màn (handoff bundle).
Done trước autoloop: M1–M4 + cosmetic pass (`7eb4a4d`).

## Autoloop M5 → M31 (tự chạy)
- [x] **M5 Finance** ✅ `7e227f8` — `/mc/finance` + `McFinanceBoard`, reuse `computeWorkspaceFinance`
- [x] **M6 Lịch** ✅ — `/mc/lich` + `McCalendarBoard`; Nhân sự (reuse `getAdminAvailabilityWeek` read-only) + Deadline (Task, week/month); sửa lịch bắc cầu GĐ1
- [x] **M7 Hộp thư** ✅ — `/mc/requests` = MC-shell bọc `RequestsInbox` (reuse getClientRequests + accept/reject/Velox); rail Inbox repoint → /mc/requests
- [x] **M8 Tệp/Review** ✅ — `/mc/tep` = MC-shell bọc `TeamBrowser` (reuse toàn bộ 3-pane review; thêm prop `chromeless` ẩn title nội bộ; admin-gated). Repoint Clapperboard → /mc/tep trên 6 màn MC. tsc+build xanh, route trong tree.
- [x] **M9 Thành viên** ✅ — `/mc/members` + `McMembersBoard`; roster `getProfileMembers` + metric `wp.user.findMany` (lương gộp VND server-side, rank+errorRate, presence, active count relative-max); reuse `InviteToProfileModal`; wire M1 UsersRound → /mc/members. tsc+build xanh.
- [x] **M10 Add Task** ✅ — `/mc/add` + `McAddScreen`; reuse `AddTaskModal` qua `DashboardActionWrapper` (controlled+hideBar, money-safe submit routing) trên backdrop MC; đóng → /mc. tsc+build xanh.
- [x] **M11 Player** ✅ — `/mc/asset/[assetId]` mirror của player GĐ1 (reuse `ReviewPlayerShell` full-bleed); thêm prop `playerBase` cho `TeamBrowser` → /mc/tep mở player MC-namespace. tsc+build xanh.
- [x] **M12 CRM** ✅ — `/mc/crm` = MC-shell (rail đầy đủ, Building2 active) bọc `ClientsManagerPanel` (reuse toàn bộ orchestrator: danh sách ↔ chi tiết ↔ tạo hóa đơn ↔ sổ thu tiền · kéo-gộp brand con · tách ra · ghi nhận thu · link chia sẻ). Panel tự mang header/breadcrumb khớp M12 → không thêm header MC. Admin-gated fail-closed; getClients cổng verifyFinanceAccess. tsc+build xanh, route trong tree.
- [x] **M13 Tạo hóa đơn** ✅ — `/mc/hoa-don` (full-bleed, không rail — màn tác vụ tập trung như M10) = `McInvoiceBoard` bọc `InvoiceModal` (embedded): trái task-picker + thuế/trả trước/hồ sơ TT, phải live PDF preview. InvoiceModal tự hydrate task chưa xuất + billing profiles + giá. Cần 1 clientId → picker khách khi vào thẳng, hoặc `?clientId=X` (từ nút CRM). Đóng → /mc/crm. Admin-gated fail-closed; DTO khách chỉ id/name/depositBalance (không jobPriceUSD). tsc+build xanh.
- [x] **M14 Hồ sơ thanh toán** ✅ — M14 vốn là MODAL mở từ M13 ("Quản lý hồ sơ") → đã sống sẵn trong `InvoiceModal` ở /mc/hoa-don. Thêm `/mc/ho-so-thanh-toan` = màn độc lập bọc `BillingProfileManager` (reuse: list ⇄ form tạo/sửa, getBillingProfiles + CRUD). Thêm props defaulted-off `open`/`onOpenChange`/`hideTrigger` vào BillingProfileManager (controlled-open) → GĐ1 byte-identical. Đóng → /mc/hoa-don. Admin-gated. tsc+build xanh.
- [x] **M15 Hồ sơ cá nhân** ✅ — `/mc/ho-so` = MIRROR composition của /dashboard/profile trong vỏ MC (reuse AvatarUpload + ProfileForm + NotificationSettings + PaymentQrUpload; self-service actions đã hardened IDOR) + dải "Chỉ số cá nhân" read-only (task hoàn tất + rank + lỗi TB thật, qua getWorkspacePrisma). User = bản ghi global (prisma). Admin-gated fail-closed. Đóng → /mc. tsc+build xanh.
- [x] **M16 Vận hành bảng task** ✅ — `/mc/board` = MC-shell (rail đầy đủ, ListTodo active + header "Vận hành bảng task" + đếm task) bọc `TaskWorkflowTabs` THẬT (isAdmin) — bảng vận hành đầy đủ mọi status, tái dùng nguyên 4 tác vụ M16 đã wire sẵn bên trong: (1) dropdown status nhóm-theo-phase + dialog "Phân loại Revision" (StatusCell), (2) menu ⋯ (Sao chép ID · Sửa chi tiết · Trả lại · Xoá cứng), (3) thanh bulk (Sửa hàng loạt→BulkEditTaskModal · Xoá) + Giao hàng loạt (AssigneeCell). Fetch y hệt /admin (task+users, assigned-first). Wire M1: "+N nữa" mỗi cột dashboard → /mc/board. Admin-gated fail-closed. tsc+build xanh, route trong tree.
- [x] **M17 Sửa hàng loạt** ✅ (delivered-via-M16) — frame M17 CHÍNH LÀ `BulkEditTaskModal` (4 tab Quy trình/Chính/Tài nguyên/Ghi chú + dirty-tracking: không đụng=giữ · gõ-rồi-xóa=CLEAR · set=UPDATE; gọi bulkUpdateTaskStatus/bulkUpdateTaskDetails). Modal đã wire sẵn trong thanh bulk của `TaskWorkflowTabs` → **đã sống ở /mc/board** (bấm "Sửa hàng loạt"). Bulk-edit vốn cần selection trên bảng → KHÔNG route độc lập (như M14 sống trong M13). 0 code mới; /admin byte-identical.
- [x] **M18 Phiên Chợ Task** ✅ — wire nút **Store** ở `McTopbarActions` (trước là `<div>` presentational) → mở modal `TaskMarketplace` THẬT (đã mount toàn cục ở `[workspaceId]/layout.tsx` `triggerMode="event"`) qua sự kiện `window 'open-marketplace'`. Modal tự fetch (`getMarketplaceTasks`), claim atomic (`claimTask`), kéo-thả nhận, trả-lại-10-phút — 0 rebuild. Badge đếm live (nghe `'marketplace-task-count'`) + entry ⌘K "Phiên Chợ Task" (accessibility). **Money-safe sẵn**: `getMarketplaceTasks` có guard [Security P0] chỉ trả `wageVND`, KHÔNG jobPriceUSD/profitVND. tsc+build xanh.
- [x] **M19 Sửa task** ✅ — hướng kết hợp trong `McTaskDrawer` (M3): **sửa nhanh trạng thái** ngay drawer (đã có: dropdown status nhóm-phase + updateTaskStatus) + **"Sửa đầy đủ"** = CTA nổi bật mới → mở editor THẬT `TaskDetailRoute` (`/[ws]/task/[taskId]`, `loadTaskDetail` money-sanitize + fail-closed) với dữ liệu điền sẵn: deadline, người làm, Giá khách $, Thù lao ₫, loại, người quản lý, brief, tài nguyên, Hook Map, ghi chú. **KHÔNG rebuild inline-edit tiền** — editor vetted đó là nơi DUY NHẤT enforce khóa-field-khi-đã-trả-lương + audit before/after (safety-protocol: cấm dựng lại logic tiền). `AddTaskModal` không có edit-mode → full-edit dùng TaskDetailRoute. tsc+build xanh.
- [x] **M20 Velox Quick Create + Hook Map** ✅ (delivered-via-M10) — Velox là mode nội bộ của `AddTaskModal` (`QuickCreateMode` + `VeloxConflictDialog` + `VeloxRawFootagesModal` + `HookGraphEditor` Multi-Hook Map + 11 toggle ⚙ + deep-scan V3/V4 + brief banner + nháp tự lưu). M10 đã reuse nguyên `AddTaskModal` qua `DashboardActionWrapper` ở **/mc/add** (submit routing y hệt /admin: createTask/batch/Velox V1+V3/Multi-Hook Map) → toàn bộ M20 **đã sống ở /mc/add** (bật toggle Velox). AddTaskModal còn có prop seed "mở thẳng Velox với folder link" (đường forward sẵn). 0 code mới; /admin byte-identical.
- [x] **M21 Shares (Link chia sẻ)** ✅ — `/mc/shares` bọc `SharesTable` THẬT (danh sách + lọc trạng thái · ⋯ Sao chép URL/Chỉnh sửa/Tắt-Bật/Xóa · `ShareLinkModal`: toggle bình luận/tải/tải-khi-duyệt/mọi-version + mật khẩu + hạn + create-then-copy + rowVersion). Tự fetch `/api/review/shares` (server: ADMIN thấy hết · USER chỉ link mình + task được giao; Xóa cổng ADMIN). Thêm prop `backHref` (SharesTable) + `sharesHref`/`trashHref` (TeamBrowser toolbar) — defaulted-off → GĐ1 byte-identical; /mc/tep truyền sharesHref=/mc/shares, back→/mc/tep. Module review KHÔNG field tiền. Admin-gated. tsc+build xanh, route trong tree.
- [x] **M22 Thùng rác Tệp + Quản lý phiên bản** ✅ — `/mc/trash` bọc `TeamTrash` THẬT (danh sách xóa 30 ngày · khôi phục theo lô · Xóa vĩnh viễn=ADMIN ConfirmModal · badge "Còn N ngày" · "gốc đã mất" · cap 200/lần). Tự fetch /api/review/trash + /restore + /purge (purge re-verify ADMIN). Thêm prop `backHref` (TeamTrash) + truyền `trashHref=/mc/trash` cho TeamBrowser toolbar (prop đã thêm ở M21). **Quản lý phiên bản** (ManageVersionsModal: stack append-only, click row→player ?v=, Tách/Xóa version) sống trong TeamBrowser/player → **đã reachable ở /mc/tep + /mc/asset** (M8/M11). Module review 0 field tiền. Admin-gated. tsc+build xanh, route trong tree.
- [x] **M23 So sánh phiên bản** ✅ — chế độ compare (`CompareView` + `useSyncedTransport`: 2 player master-follower, bên active có tiếng+comment, `?cmp=v1.v2` là source-of-truth, Back=thoát) sống TRONG `ReviewPlayerShell` → **đã render sẵn ở /mc/asset?cmp=** (M11 truyền compareParam). Fix điều hướng namespace: `compareBase` (nút "So sánh phiên bản" + đổi version + viết ?cmp) và back-to-Tệp trước HARDCODE `/team` → thêm 2 prop defaulted-off `assetBasePath` + `browserHref` vào ReviewPlayerShell (mặc định /team → GĐ1 byte-identical); /mc/asset truyền `/mc/asset` + `/mc/tep` → compare & thoát Ở LẠI namespace MC (cũng vá luôn back-nav M11). Module review 0 field tiền. tsc+build xanh.
- [x] **M24 Bảng lương: thưởng + khóa sổ + thanh toán** ✅ — cụm tiền nhạy nhất, MAP kỹ bằng workflow 5-agent trước khi build (money-safety ground-truth). Thêm `<BonusCalculator workspaceId>` THẬT vào header `McPayrollBoard` (`c1e054c`) → **tính thưởng + TỰ KHÓA SỔ (PayrollLock) + ⚙ BonusConfigModal NATIVE** (trước bridge sang GĐ1). Component tự fetch lock, confirm-gate `calculateMonthlyBonus` (huỷ diệt+auto-lock) qua ConfirmProvider (ở root layout → chạy dưới /mc). **Thanh toán**: giữ luồng `confirmPayment` 2-bước SẴN CÓ của board (đã money-safe, cùng action như PaymentModal) — QR PaymentModal (bản xem bank/QR trước khi xác nhận) **CHỦ ĐÍCH HOÃN**: không thay luồng-tiền đang chạy tốt dưới ngữ cảnh nặng (an-toàn-là-trên-hết); logic pay y hệt. Money-safe tuyệt đối: mọi action `verifyWorkspaceAccess ADMIN`, VND-only (revenue=Σ task.value, KHÔNG jobPriceUSD), BonusCalculator render 0 tiền. /mc/tien admin-gated fail-closed. tsc+build xanh.
- [x] **M25 CRM tiền (ghi nhận thu · hóa đơn · share portal)** ✅ `6e95c14` — **cấu trúc delivered-via-M12** (/mc/crm đã reuse trọn `ClientsManagerPanel`: RecordPaymentModal "sổ thu tiền" + ClientInvoicesTable + ShareLinkSection + thêm khách + kéo-gộp/tách). Net-new an toàn (gate defaulted-off `missionControl` trên panel → GĐ1 /admin/crm + DashboardClientsRow byte-identical): (2) **RecordPaymentModal `confirmOnDelete`** → confirm (useConfirm) trước khi XÓA CỨNG 1 lần thu (vá footgun "xóa thẳng" thiết kế nêu); (3) **StatusPill `clarify`** → nhãn PENDING (khách có task & tất cả 'Hoàn tất') đổi từ amber "Chờ xử lý" (nghĩa ngược) → indigo "Đã xong hết". **Money-safe**: 0 đổi server/action/schema, verifyFinanceAccess+ADMIN nguyên, 0 đổi surface jobPriceUSD. **HOÃN** (ghi DECISIONS): (1) "Gắn vào hóa đơn" auto-PAID — cần số học coverage + trả góp + revert-on-delete (recordPayment hiện flip PAID vô điều kiện khi có invoiceId); (4) ShareLink "Link Settings" toggles — resolveShareToken (portal /share) KHÔNG đọc allowDownload/canApprove/passphraseHash → expose = control giả/passphrase giả (BẢO MẬT). tsc+build xanh, route trong tree.
- [x] **M26 Thùng rác hợp nhất (4 tab)** ✅ `bfbfd14` — `/mc/trash` thành **hub 4 tab** GỘP (Màn 26 "đã duyệt gộp"), **SUBSUME** /mc/trash cũ (M22, chỉ Tệp) làm tab 1. **Reuse-first tuyệt đối** — mỗi tab mount 1 surface trash SẴN CÓ, 0 dựng lại logic: **Tệp**→`TeamTrash` (chromeless) · **Khách**→`ClientTrashClient` (getTrashedClients/restoreClient/permanentlyDeleteClient — xóa-vĩnh-viễn **BỊ CHẶN khi còn hóa đơn liên kết**, guard server đếm invoice toàn hệ) · **Task đã hủy**→`CancelledTasksClient` (getCancelledTasks/restoreCancelledTask) · **Tổ chức**→`ProfileTrashClient` (getMyTrashedProfiles đã tự lọc OWNER→non-owner rỗng). Mới: `McTrashHub` (client, tab bar, chỉ mount tab active). `TeamTrash` +prop `chromeless` defaulted-off → /team/trash byte-identical. Data tab 2/3/4 fetch server-side (Promise.all, gated); Tệp tự fetch. Wire rail Trash2 (bare)→/mc/trash ở /mc/board + /mc/crm. **Money-safe**: admin-gated fail-closed; DTO khách = counts (0 jobPriceUSD); restore/purge/permanent re-verify server. **GAP ghi DECISIONS**: "khôi phục task về status TRƯỚC khi hủy" bất khả (không cột statusBeforeCancel; restoreCancelledTask chuẩn-hóa về Nhận task/Đang đợi giao — đúng behavior GĐ1, reuse nguyên). tsc+build xanh, route trong tree.
- [ ] M27–M31 (stubs — bám spec DAC-TA-CHUC-NANG-ADMIN.md)

Kế tiếp: **M27–M31** (stubs — bám spec `docs/DAC-TA-CHUC-NANG-ADMIN.md`). Rail MC còn 2 mục title-only chưa route: **Activity "Phân tích — Màn 28"** + **ScrollText "Nhật ký hoạt động — Màn 29"** (đã có surface GĐ1: admin Nhật ký hoạt động/AuditLog từ P6.3 — reuse-first như M26). M27/M30/M31 = đọc frame + map spec khi tới.
**M27–M31 resume-note:** cùng công thức đã chạy 22 màn: đọc frame `.dc.html` (M26 ở line 3986; M27+ tiếp sau) → map surface GĐ1 (Explore nếu rộng) → reuse-first (bọc component vetted trong vỏ MC rail + defaulted-off props giữ GĐ1 byte-identical) → data-wired, money-safe (admin-gated fail-closed, 0 rò jobPriceUSD), tsc+build → commit (commit-only KHÔNG push) → docs. Màn 28 (Phân tích) + 29 (Nhật ký hoạt động) nhiều khả năng reuse dashboard-analytics + AuditLog feed sẵn có.
**M24 resume-note (đã map):** `/mc/tien` + `McPayrollBoard` ĐÃ có sẵn = board data-wired (Thực nhận Σ Hoàn tất+bonus, `confirmPayment`/`revertPayment` 2-step, admin-gated, money DTO server-side). M24 CẦN THÊM lên board đó, tái dùng component GĐ1 vetted (KHÔNG rebuild logic tiền): **BonusCalculator** ("Tính Thưởng Tháng Này" → `calculateMonthlyBonus` TỰ KHÓA kỳ = PayrollLock) + **BonusConfigModal** (⚙ % top theo tổ chức, đã có schema+actions #133-135) + **PaymentModal** (QR tĩnh + bank + "Xác nhận đã chuyển khoản") + chỉ báo **khóa sổ** (revert bị chặn khi locked). Map `src/components/payroll/*` (BonusCalculator, BonusConfigModal, PaymentModal, PayrollCard) trước — reuse controlled-open như M14/M10. Money-safe tuyệt đối: chỉ admin, không rò jobPriceUSD, calculateMonthlyBonus/lock chỉ TRIGGER qua action vetted (không tái tính tay).
Mỗi màn: đọc frame → map surface (reuse-first) → build data-wired money-safe admin-gated → tsc+build → commit → docs.
