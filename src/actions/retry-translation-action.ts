'use server'

/**
 * @deprecated Auto-translation is disabled in favor of manual entry.
 */
export async function retryTaskTranslation(taskId: string, workspaceId: string) {
    return { error: 'Tính năng dịch tự động đã bị gỡ bỏ để tối ưu chi phí. Vui lòng nhập bản dịch thủ công.' }
}
